[package]
name = "roml-mosek"
version = "0.1.0"
edition = "2021"
description = "MOSEK solver adapter for roml"
publish = false

[dependencies]
log = "0.4.29"
log4rs = "1.4.0"
roml = { path = ".." }
serde_yaml = "0.9"

[build-dependencies]
