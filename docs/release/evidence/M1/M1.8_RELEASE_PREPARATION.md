# M1.8 — Release Candidate and Publication

**Date:** 2026-07-14
**Status:** Preparation; blocked on M1.4-M1.5 CI execution and owner authorization

## Publication Order

1. `roml` (core) — already verified, 0.1.x crate
2. `roml-highs` — after M1.4 qualification, 0.1.x crate
3. `roml-mosek` — remains `publish = false`
4. `roml-xpress` — remains `publish = false`

## Pre-Publication Checklist

| Check | roml | roml-highs |
|-------|------|-----------|
| `cargo fmt --all -- --check` | ✅ | 🟡 |
| `cargo clippy -p <crate> --all-targets -- -D warnings` | ✅ | 🟡 |
| `cargo test -p <crate> --all-targets` | ✅ 333 tests | 🟡 |
| `cargo doc -p <crate> --no-deps` | ✅ | 🟡 |
| `cargo package -p <crate> --no-verify` | ✅ | 🟡 |
| `cargo deny check` | ✅ | 🟡 |
| `cargo audit` | ✅ | 🟡 |
| `cargo machete` | ✅ | 🟡 |
| `cargo semver-checks` (advisory) | 🟡 | 🟡 |
| 3-OS CI green | ✅ core only | 🟡 |
| MSRV CI green | ✅ 1.85 | 🟡 |
| Package consumer test | ✅ | 🟡 |

## Version Policy

- Both crates start at `0.1.0`
- Pre-1.0 semver: breaking changes increment minor version
- Workspace version managed via `[workspace.package] version`

## Release Artifacts

- Git tag: `roml-v0.1.0`, `roml-highs-v0.1.0`
- GitHub Release with changelog excerpts
- crates.io publication via `cargo publish`

## Authorization Gate

- No publication without explicit owner authorization for exact SHAs
- Publication is a manual step (not automated in CI)
- `roml-mosek` and `roml-xpress` remain `publish = false`

## Post-Publication Verification

- docs.rs build check (roml should build; roml-highs may need native libs)
- crates.io install test: `cargo add roml` in fresh project
- Git tags pushed and GitHub Release created
