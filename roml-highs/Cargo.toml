[package]
name = "roml-highs"
version = "0.1.0"
edition = "2021"
description = "HiGHS solver adapter for roml"

[dependencies]
log = "0.4.29"
log4rs = "1.4.0"
roml = { path = ".." }
serde_yaml = "0.9"

[build-dependencies]
cmake = "0.1"

[dev-dependencies]
