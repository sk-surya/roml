[package]
name = "roml-highs"
version = "0.1.0"
edition = "2021"
description = "HiGHS solver backend for roml"

[dependencies]
highs-sys = "1.15"
log = "0.4"
roml = { path = ".." }

[features]
default = ["bundled"]
bundled = []
system = []
