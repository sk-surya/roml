---
phase: 10-backend-contract-migration-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02-PLAN.md
files_modified:
  - src/solver/mod.rs
  - src/lib.rs
  - src/solution/mod.rs
  - src/model/mod.rs
  - .planning/adr/ADR-001-backend-contract-freeze.md
  - .planning/STATE.md
autonomous: true
requirements:
  - M1R-C7
  - M1R-C8
user_setup: []

must_haves:
  truths:
    - "SolverAdapter, SolverModelExt, SolverStatus, LpAlgorithm, SolveOptions are REMOVED from src/solver/mod.rs"
    - "Solution::status uses TerminationStatus instead of SolverStatus"
    - "SolutionBuilder::status() accepts TerminationStatus"
    - "All tests (lib, integration) compile and pass with no references to removed types"
    - "src/lib.rs no longer re-exports SolverAdapter, SolverModelExt, SolverStatus, LpAlgorithm, SolveOptions"
    - "src/lib.rs prelude no longer includes SolverAdapter, SolverModelExt, SolverStatus"
    - "Change is no longer re-exported from lib.rs (stays internal to crate)"
    - "No test file has `#[ignore]` annotations (all non-ignored tests either pass or are deleted)"
    - "ADR-001-backend-contract-freeze.md exists with frozen trait/type signatures and freeze commit SHA"
    - "STATE.md Phase M1R-01 row references the ADR and freeze SHA"
  artifacts:
    - .planning/adr/ADR-001-backend-contract-freeze.md
  key_links:
    - "src/solver/mod.rs after cleanup: callback + backend + reference + request + session + SolverError"
    - "src/lib.rs public API exports the new protocol types, NOT legacy ones"
    - "ADR references exact commit SHA for contract freeze"
---

<objective>
Remove all legacy solver APIs from the public surface, update remaining type references, verify no ignored tests remain, and freeze the contract with a lightweight ADR.

Purpose: Complete D2 (legacy removal) and D5 (contract freeze). After this plan, the `roml` core crate has zero legacy backend types in its public API. The `BackendSession` trait hierarchy is the ONLY supported execution path. The contract freeze ADR marks this commit SHA as the immutable baseline for downstream backend implementations.

Output: Cleaned solver/mod.rs with only modern types; Solution migrated to TerminationStatus; updated lib.rs and prelude; contract freeze ADR at .planning/adr/ with commit SHA reference in STATE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (M1R-01 section, gate)
@.planning/REQUIREMENTS.md (M1R-C7, M1R-C8)
@.planning/STATE.md (phase ledger)
@.planning/DECISIONS.md (D-002, D-003, D-005)

# Locked decisions for removal
@.planning/phases/10-backend-contract-migration-closure/10-CONTEXT.md (D2 removal list, D4 architecture, D5 ADR process)

# Current source after Plan 02
@src/solver/mod.rs
@src/lib.rs
@src/solution/mod.rs
@src/model/mod.rs
@src/solver/backend.rs
@src/solver/request.rs
@src/solver/session.rs

# Current tests (should all pass with legacy types present before cleanup)
@tests/changelog_integration.rs
@tests/backend_contract.rs
@tests/macro_api.rs
@src/model/mod.rs (inline tests)

# Relevant references
@src/solver/callback.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove legacy SolverAdapter/SolverModelExt/SolverStatus/LpAlgorithm/SolveOptions from solver/mod.rs</name>
  <files>
    src/solver/mod.rs
  </files>
  <read_first>
    @src/solver/mod.rs (full file — all current content including legacy types and inline tests)
    @src/solver/callback.rs (unchanged — verify no cross-references)
    @src/solver/backend.rs (TerminationStatus — replaces SolverStatus)
    @src/solver/request.rs (SolveRequest — replaces SolveOptions, LpAlgorithm moves here)
  </read_first>
  <action>
    Rewrite src/solver/mod.rs to remove all legacy types. The file becomes a module collector and re-exporter for the new backend contract types. Keep `SolverError` as-is (it stays for backend crate compatibility per D2).

    AFTER cleanup, src/solver/mod.rs must contain ONLY:

    1. Module doc comment (update to no longer say "Solver adapter traits" — say "Backend session contract types and error types")
    2. `pub mod callback;` (unchanged)
    3. `pub mod backend;` (from Plan 01)
    4. `pub mod reference;` (from Plan 02)
    5. `pub mod request;` (from Plan 01)
    6. `pub mod session;` (from Plan 01)
    7. `SolverError` enum preserved exactly as-is (same 3 variants: NotSupported, InternalError, InvalidModel) — backend crates import it by name
    8. Remove: `SolverStatus` enum entirely (replaced by `TerminationStatus` in backend.rs)
    9. Remove: `LpAlgorithm` enum entirely (lives in `request.rs` as part of `SolveRequest` and `EffectiveConfig`)
    10. Remove: `SolveOptions` struct entirely (replaced by `SolveRequest`)
    11. Remove: `SolverAdapter` trait entirely (all 13 methods)
    12. Remove: `SolverModelExt` trait entirely (blanket impl and solve_model method)
    13. Remove: `impl<T> SolverModelExt for T where T: SolverAdapter + ?Sized {}` blanket impl
    14. Remove: entire `#[cfg(test)] mod tests` block at the bottom (it tests the MockAdapter + SolverAdapter + SolverModelExt — all being removed)

    Keep the import line for any types still needed by SolverError:
    ```rust
    use crate::id::{ConId, VarId};
    ```
    Only keep imports that SolverError actually uses (it doesn't need Model, Solution, etc.).

    Destination for moved types:
    - LpAlgorithm: Already defined in src/solver/request.rs (as part of EffectiveConfig). The worktree's request.rs imports `use crate::solver::LpAlgorithm;` — this will break because we're removing LpAlgorithm from solver/mod.rs. Fix: Add `LpAlgorithm` to src/solver/request.rs (define it there, or add a `pub use` re-export from request.rs). The cleanest approach: define `LpAlgorithm` in `src/solver/request.rs` (it's conceptually part of solve configuration) and re-export from `solver/mod.rs`: `pub use request::LpAlgorithm;`. Verify worktree request.rs's `use crate::solver::LpAlgorithm;` — this path won't resolve if LpAlgorithm moved. Check if request.rs currently re-exports it. Better: keep a thin `pub use` in solver/mod.rs:
      ```rust
      // LpAlgorithm is defined in the request module as part of solve configuration
      pub use request::LpAlgorithm;
      ```
    - Same for any type that was previously in solver/mod.rs but is now in request.rs or backend.rs.

    Compile check after cleanup:
    - `cargo test -p roml --lib`
    - Expected: errors in `src/solution/mod.rs` (`SolverStatus` not found) — this is expected and will be fixed in the next task
    - Expected: errors in `src/model/mod.rs` inline tests (`SolverStatus` not found) — will be fixed in next task
    - Expected: `SolverError` still compiles (it stays)
    - Expected: all new protocol modules compile (backend, request, session, reference)
  </action>
  <acceptance_criteria>
    <source_assertion>SolverAdapter trait removed from solver/mod.rs</source_assertion>
    <source_assertion>SolverModelExt trait and blanket impl removed</source_assertion>
    <source_assertion>SolverStatus enum removed</source_assertion>
    <source_assertion>LpAlgorithm removed as standalone enum (re-exported from request.rs)</source_assertion>
    <source_assertion>SolveOptions struct removed</source_assertion>
    <source_assertion>SolverError preserved unchanged</source_assertion>
    <source_assertion>#[cfg(test)] mod tests block with MockAdapter removed</source_assertion>
    <source_assertion>All new module declarations (backend, reference, request, session) present</source_assertion>
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c 'pub trait SolverAdapter' src/solver/mod.rs; test $? -eq 1
      grep -c 'pub trait SolverModelExt' src/solver/mod.rs; test $? -eq 1
      grep -c 'pub enum SolverStatus' src/solver/mod.rs; test $? -eq 1
      grep -c 'pub enum LpAlgorithm' src/solver/mod.rs; test $? -eq 1
      grep -c 'pub struct SolveOptions' src/solver/mod.rs; test $? -eq 1
      grep -c 'pub enum SolverError' src/solver/mod.rs; test $? -eq 0
    </automated>
  </verify>
  <done>
    solver/mod.rs cleaned: SolverAdapter/SolverModelExt/SolverStatus/LpAlgorithm/SolveOptions removed, SolverError preserved, modules declared
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate Solution to TerminationStatus, update lib.rs/prelude, verify no ignored tests, write ADR</name>
  <files>
    src/solution/mod.rs
    src/lib.rs
    src/model/mod.rs
    .planning/adr/ADR-001-backend-contract-freeze.md
    .planning/STATE.md
  </files>
  <read_first>
    @src/solution/mod.rs (full file — Solution, SolutionBuilder, all inline tests)
    @src/lib.rs (full file — all re-exports and prelude)
    @src/model/mod.rs (inline tests that reference SolverStatus)
    @src/solver/backend.rs (TerminationStatus)
    @.planning/phases/10-backend-contract-migration-closure/10-CONTEXT.md (D2 removal list, D5 ADR process)
  </read_first>
  <action>
    Perform these sub-tasks in order:

    Sub-task 2a: Migrate src/solution/mod.rs to TerminationStatus
    - Change `use crate::solver::SolverStatus;` to `use crate::solver::backend::TerminationStatus;`
    - Change `Solution::status` field type from `SolverStatus` to `TerminationStatus`
    - Change `SolutionBuilder` field `status` from `SolverStatus` to `TerminationStatus`
    - Change `Solution::new(status: SolverStatus)` to `Solution::new(status: TerminationStatus)`
    - Change `Solution::from_values(...)` similarly
    - Change `Solution::status()` return type from `SolverStatus` to `&TerminationStatus`
    - Change `Solution::is_optimal()` to match on `TerminationStatus::Optimal` instead of `SolverStatus::Optimal`
    - Change `SolutionBuilder::status(mut self, status: SolverStatus)` to accept `TerminationStatus`
    - In all `#[cfg(test)]` tests in solution/mod.rs, replace `SolverStatus::Optimal` with `TerminationStatus::Optimal`
    - Keep `SolverStatus` as a type alias if needed for backward compat: `#[allow(deprecated)] pub use backend::TerminationStatus as SolverStatus;` — NO, do NOT do this. D2 says remove. Just migrate everything.
    - Verify: `cargo test -p roml --lib` passes (solution tests work with TerminationStatus)

    Sub-task 2b: Update src/model/mod.rs inline tests
    - Find all references to `SolverStatus` in the model tests (lines 1146, 1184, 1221, 1256) and the user of `drain_changes` in test (line 970)
    - Replace `use crate::solver::SolverStatus;` with `use crate::solver::backend::TerminationStatus;`
    - Replace `SolverStatus::Optimal` with `TerminationStatus::Optimal`
    - Replace other references as appropriate
    - The test `changelog_tracking` (line 962) calls `model.drain_changes()` — this was fixed in Plan 02 Task 1. Verify it uses the new API.

    Sub-task 2c: Update src/lib.rs exports
    - Change the `pub use solver::` line: remove `LpAlgorithm, SolveOptions, SolverAdapter, SolverModelExt, SolverStatus`. Keep only `SolverError`:
      ```rust
      pub use solver::{SolverError};
      ```
    - Change the prelude to remove `SolverAdapter, SolverModelExt, SolverStatus`. Keep `SolverError`.
    - Remove `pub use model::changelog::Change;` from lib.rs — Change stays internal to crate
    - Add new re-exports for the protocol types. The following should be exported from crate root for consumer access:
      ```rust
      pub use delta::{DeltaBatch, ModelOp};
      pub use revision::ModelRevision;
      pub use snapshot::ModelSnapshot;
      pub use solver::backend::{
          BackendCapabilities, BackendError, BackendInfo,
          ErrorCategory, HealthEffect, TerminationStatus,
      };
      pub use solver::request::{
          ConfigAdjustment, ConfigRejection, EffectiveConfig,
          SolveRequest, SolveResult, SolveSolution,
      };
      pub use solver::session::{
          BackendMetadata, BackendSession, CallbackSession,
          SessionHealth, SolutionView, SyncReceipt, Synchronization,
      };
      pub use sync::{AdapterCursor, AdapterHealth, ApplyOutcome};
      pub use solver::LpAlgorithm;
      ```
    - Also update the prelude to include the commonly-used new types (SolveRequest, SolveResult, TerminationStatus, BackendSession, etc.)

    Sub-task 2d: Verify no `#[ignore]` annotations remain in test files
    - Run: `grep -rn '#\[ignore\]' tests/ src/ 2>/dev/null | grep -v target`
    - If any `#[ignore]` tests exist, each must be:
      1. Removed (with deletion or lifting the ignore), OR
      2. Accompanied by a documented rationale for why it needs to stay ignored (external blocker per M1R-C8)
    - Expected: zero `#[ignore]` annotations in tests/ and src/

    Sub-task 2e: Run full verification suite
    ```bash
    cargo test -p roml --all-targets
    cargo clippy -p roml --all-targets -- -D warnings
    RUSTDOCFLAGS='-D warnings' cargo doc -p roml --no-deps
    ```
    Fix any warnings or errors. The full suite must pass clean.

    Sub-task 2f: Write ADR-001-backend-contract-freeze.md
    Create `.planning/adr/ADR-001-backend-contract-freeze.md` with this structure:

    ```markdown
    # ADR-001: Backend Contract Freeze

    **Date:** 2026-07-18
    **Phase:** M1R-01 (Phase 10)
    **Freeze Commit SHA:** `{current git commit hash}`

    ## Frozen Traits

    ### BackendSession (required — every backend implements)
    - `synchronize(&mut self, sync: Synchronization) -> Result<SyncReceipt, BackendError>`
    - `solve(&mut self, request: &SolveRequest) -> Result<SolveResult, BackendError>`
    - `close(self) -> Result<(), BackendError>`

    ### SessionHealth (optional)
    - `health(&self) -> AdapterHealth`
    - `revision(&self) -> ModelRevision`

    ### SolutionView (optional)
    - `value(&self, var: VarId) -> Option<f64>`
    - `dual(&self, con: ConId) -> Option<f64>`
    - `reduced_cost(&self, var: VarId) -> Option<f64>`
    - `objective_value(&self) -> Option<f64>`

    ### CallbackSession (optional — MIP backends)
    - `set_callback_handler(handler: Box<dyn CallbackHandler>) -> Result<(), BackendError>`
    - `clear_callback_handler() -> Result<(), BackendError>`

    ### BackendMetadata (optional)
    - `name(&self) -> &str`
    - `capabilities(&self) -> BackendCapabilities`

    ## Frozen Types
    - SolveRequest, SolveResult, TerminationStatus (11 variants), EffectiveConfig, SolveSolution
    - BackendError, ErrorCategory, HealthEffect, BackendCapabilities
    - Synchronization, SyncReceipt, DeltaBatch, ModelOp (16 variants)
    - ModelRevision, AdapterCursor, AdapterHealth, ApplyOutcome, Journal, ModelSnapshot

    ## Change Process
    Any post-freeze edit to a frozen trait or type signature requires:
    1. A recorded decision (update to this ADR or a new ADR).
    2. Notification to all downstream workers (HiGHS, MOSEK, Xpress).
    3. Coordinated rebase of all downstream branches.
    ```

    Sub-task 2g: Update STATE.md
    Add a row to the phase ledger for M1R-01:
    ```markdown
    | M1R-01 Backend contract migration closure | Complete | Gate: PASS. Freeze SHA: {SHA}. ADR-001. |
    ```

    If the STATE.md Phase Ledger section has a placeholder for M1R-01, update it with the completion status and reference.
  </action>
  <acceptance_criteria>
    <source_assertion>src/solution/mod.rs uses TerminationStatus instead of SolverStatus throughout</source_assertion>
    <source_assertion>src/lib.rs does NOT re-export SolverAdapter, SolverModelExt, SolverStatus, LpAlgorithm, SolveOptions, Change</source_assertion>
    <source_assertion>src/lib.rs DOES re-export new protocol types (BackendSession, SolveRequest, TerminationStatus, etc.)</source_assertion>
    <source_assertion>No #[ignore] annotations in tests/ or src/ (grep returns exit code 1)</source_assertion>
    <source_assertion>.planning/adr/ADR-001-backend-contract-freeze.md exists with frozen signatures and commit SHA</source_assertion>
    <source_assertion>`cargo test -p roml --all-targets` passes</source_assertion>
    <source_assertion>`cargo clippy -p roml --all-targets -- -D warnings` passes</source_assertion>
    <source_assertion>`cargo semver-checks check-release -p roml` passes (or at least runs without crash — "no preceding release" is acceptable for v0.1)</source_assertion>
  </acceptance_criteria>
  <verify>
    <automated>
      cargo test -p roml --all-targets 2>&1 | tail -20
      grep -rn '#\[ignore\]' tests/ src/ 2>/dev/null; test $? -eq 1
    </automated>
  </verify>
  <done>
    Legacy types removed from public API, Solution migrated to TerminationStatus, ADR-001 freezes contract, full verification suite passes
  </done>
</task>

</tasks>

<verification>
```bash
# Full verification (phase gate)
cargo test -p roml --all-targets
cargo test -p roml --test backend_contract
cargo clippy -p roml --all-targets -- -D warnings
RUSTDOCFLAGS='-D warnings' cargo doc -p roml --no-deps
cargo semver-checks check-release -p roml

# No legacy types remain in public API
grep -c 'SolverAdapter' src/lib.rs; test $? -eq 1
grep -c 'SolverModelExt' src/lib.rs; test $? -eq 1
grep -c 'SolverStatus' src/solver/mod.rs; test $? -eq 1
grep -c 'LpAlgorithm' src/solver/mod.rs; test $? -eq 1
grep -c 'pub struct SolveOptions' src/solver/mod.rs; test $? -eq 1
grep -c 'pub use model::changelog::Change' src/lib.rs; test $? -eq 1

# No ignored tests remain
grep -rn '#\[ignore\]' tests/ src/; test $? -eq 1

# ADR exists
ls .planning/adr/ADR-001-backend-contract-freeze.md
```
</verification>

<success_criteria>
- Legacy API types fully removed from public surface
- Solution migrated from SolverStatus to TerminationStatus
- All tests pass (lib, integration, contract)
- clippy clean with -D warnings
- Doc build clean
- semver-checks runs (no-preceding-release is acceptable for v0.1)
- No #[ignore] tests remain
- ADR-001 freezes the contract at the current commit SHA
- STATE.md updated with freeze reference
</success_criteria>

<output>
Create `.planning/phases/10-backend-contract-migration-closure/10-03-SUMMARY.md` when done
</output>
