---
phase: 09-truth-reset-and-candidate-admission
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/release/evidence/M1R/artifacts/02-claim-reconciliation.md
autonomous: true
requirements:
  - M1R-G1
must_haves:
  truths:
    - Every M1.0-M1.5 sub-claim from RESEARCH.md is cross-referenced against source code on the candidate branch
    - Each claim has a truth verdict: Accepted, Partially Satisfied, Failed, Locally Verified, or Cannot Verify
    - Legacy source patterns (drain_changes, SolverAdapter, Model.solver_options, panic constructors) are documented with file:line references and severity ratings
    - The disposition table uses STATE.md vocabulary exclusively
    - No claim disposition relies on evidence-document claims ("X complete" in a markdown file) — only source code + test output + CI evidence
  artifacts:
    - docs/release/evidence/M1R/artifacts/02-claim-reconciliation.md
  key_links:
    - M1.0 admission claim -> c4db3e0 source evidence -> Partially Satisfied / Failed
    - M1.1 contract freeze claim -> drain_changes() at src/model/mod.rs:747 -> Failed (legacy path still public)
    - M1.2 highs-sys migration -> roml-highs/Cargo.toml highs-sys 1.15.0 -> Accepted
    - M1.3 commuting square -> tests/backend_contract.rs (46 tests) ReferenceBackend only -> Partially Satisfied
    - M1.4 CI workflow -> .github/workflows/ci-highs.yml exists but never executed -> Partially Satisfied
    - M1.5 benchmark harness -> benches/model_bench.rs exists -> Partially Satisfied (no results)
---

<objective>
Cross-reference every M1.0-M1.5 completion claim against source code and test evidence on the candidate branch, producing a formal requirement disposition table using STATE.md vocabulary.

Purpose: The inherited candidate claims M1.0-M1.5 complete. This plan verifies each sub-claim by inspecting actual source files, git history, and test results — not by trusting evidence documents. The disposition table becomes the authoritative baseline for all subsequent M1R phases.
Output: docs/release/evidence/M1R/artifacts/02-claim-reconciliation.md with requirement disposition table and legacy pattern documentation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/TRACEABILITY.md
@.planning/phases/09-truth-reset-and-candidate-admission/09-RESEARCH.md (sections: M1.0-M1.5 Claim Reconciliation, Legacy Source Patterns)
@.planning/phases/09-truth-reset-and-candidate-admission/09-CONTEXT.md (D1: Admission criteria)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-reference M1.0-M1.5 claims against source evidence</name>
  <files>docs/release/evidence/M1R/artifacts/02-claim-reconciliation.md</files>
  <read_first>
    - 09-RESEARCH.md section "M1.0-M1.5 Claim Reconciliation" (every sub-claim table for M1.0 through M1.5)
    - 09-RESEARCH.md section "Overall M1 State Verdict"
  </read_first>
  <action>
    For each M1 milestone (M1.0 through M1.5), verify every sub-claim from RESEARCH.md by running concrete commands against the candidate branch (planning/roml-M1-native-backends-release). Run these commands remotely using `git show` and `git log` — do NOT switch branches.

    Verify each claim class:

    1. **Contract/type claims** (e.g., "protocol types frozen", "SolveRequest type exists"):
       - `git show planning/roml-M1-native-backends-release:src/delta.rs | head -5` — confirm file exists
       - `git grep -c 'pub (struct|enum|trait|fn)' planning/roml-M1-native-backends-release -- src/delta.rs src/revision.rs src/snapshot.rs src/sync.rs src/solver/backend.rs src/solver/request.rs` — confirm public items exist
       - `git show planning/roml-M1-native-backends-release:src/solver/request.rs | head -10` — confirm SolveRequest type

    2. **Legacy path claims** (e.g., "legacy path disposition not addressed"):
       - `git grep -n 'drain_changes' planning/roml-M1-native-backends-release -- src/model/mod.rs` — confirm drain_changes still exists
       - `git grep -rn 'SolverAdapter' planning/roml-M1-native-backends-release -- roml-highs/src/` — confirm SolverAdapter trait still in use

    3. **Migration claims** (e.g., "highs-sys migration complete"):
       - `git show planning/roml-M1-native-backends-release:roml-highs/Cargo.toml | grep highs-sys` — confirm highs-sys dependency
       - `git ls-tree -r planning/roml-M1-native-backends-release --name-only roml-highs/src/ | grep -v mod.rs` — confirm ffi.rs removed

    4. **Test claims** (e.g., "46 contract tests pass"):
       - `git ls-tree planning/roml-M1-native-backends-release -- tests/backend_contract.rs` — confirm test file exists
       - `git grep -c '#\[test\]' planning/roml-M1-native-backends-release -- tests/backend_contract.rs` — count test functions
       - `git grep -c '#\[ignore\]' planning/roml-M1-native-backends-release -- tests/` — count ignored tests

    5. **CI claims** (e.g., "CI workflow created"):
       - `git ls-tree planning/roml-M1-native-backends-release -- .github/workflows/ci-highs.yml` — confirm workflow file exists
       - NOTE: CI has never executed. Record as "locally verified" at most.

    6. **Benchmark claims** (e.g., "4 criterion benchmarks exist"):
       - `git ls-tree planning/roml-M1-native-backends-release -- benches/` — list bench files
       - `git show planning/roml-M1-native-backends-release:benches/model_bench.rs | head -20` — confirm criterion harness

    Write each verification as a row in a multi-column disposition table:

    | Milestone | Claim | Source Evidence | Command Run | Truth Verdict |
    |-----------|-------|----------------|-------------|---------------|
    | M1.0 | admission complete | docs exist, license files committed | git show, git log | Partially Satisfied |
    | M1.1 | contract freeze complete | drain_changes() still at line 747 | git grep | Failed |

    Use these Truth Verdict values exclusively (per STATE.md vocabulary):
    - **Accepted** — source evidence confirms the claim
    - **Partially Satisfied** — some evidence exists but not all requirements met
    - **Failed** — source evidence contradicts the claim or required evidence absent
    - **Locally Verified** — claim confirmed on local machine but not CI-verified
    - **Cannot Verify** — required commands or environment unavailable
    - **Owner-Blocked** — requires owner authorization

    Append the full disposition table under H2 `## M1 Claim Disposition Table`.

    Then append the RESEARCH.md summary verdict table (Overall M1 State Verdict) as a comparison reference, noting any discrepancies between the RESEARCH.md verdict and this task's fresh verification.
  </action>
  <acceptance_criteria>
    - Claim disposition table has rows for all M1.0-M1.5 milestones
    - Each sub-claim maps to a Truth Verdict using only STATE.md vocabulary
    - Every non-trivial claim was verified by a git command, not copied from RESEARCH.md
    - drain_changes() at src/model/mod.rs line ~747 confirmed still public
    - SolverAdapter trait confirmed still implemented in roml-highs
    - highs-sys dependency confirmed at 1.15.0
    - ffi.rs confirmed removed (replaced by highs-sys)
    - No Truth Verdict is based solely on an evidence-document claim
  </acceptance_criteria>
  <verify>
    <automated>grep -c '| Accepted \| Partially Satisfied \| Failed \| Locally Verified \| Cannot Verify \| Owner-Blocked' docs/release/evidence/M1R/artifacts/02-claim-reconciliation.md</automated>
  </verify>
  <done>M1.0-M1.5 claim disposition table exists with STATE.md vocabulary, each sub-claim backed by a git command or source inspection</done>
</task>

<task type="auto">
  <name>Task 2: Document legacy source patterns with file:line references</name>
  <files>docs/release/evidence/M1R/artifacts/02-claim-reconciliation.md</files>
  <read_first>
    - 09-RESEARCH.md section "Legacy Source Patterns Identified"
    - 09-CONTEXT.md (D2: Ignored test disposition context on legacy patterns)
  </read_first>
  <action>
    Append H2 section `## Legacy Source Patterns` to 02-claim-reconciliation.md. For each legacy pattern from RESEARCH.md, verify with fresh grep commands and record:

    1. **Pattern: `Model::drain_changes()` (destructive)**
       - `git grep -n 'fn drain_changes' planning/roml-M1-native-backends-release -- src/model/mod.rs`
       - Record exact line number and function signature
       - `git grep -n 'drain_changes' planning/roml-M1-native-backends-release -- src/` — count total references
       - Severity: Critical
       - Note: This is the destructive synchronization point that the revisioned protocol replaces

    2. **Pattern: `SolverAdapter` trait (public, legacy path)**
       - `git grep -rn 'impl SolverAdapter' planning/roml-M1-native-backends-release -- roml-highs/src/`
       - Record exact line number and file
       - `git grep -rn 'SolverAdapter' planning/roml-M1-native-backends-release -- roml-highs/src/adapter.rs | head -5`
       - Severity: Critical
       - Note: Legacy path is still the PUBLIC execution path for HiGHS

    3. **Pattern: `Model.solver_options` (model-owned solve policy)**
       - `git grep -n 'solver_options' planning/roml-M1-native-backends-release -- src/model/mod.rs | head -5`
       - Record field type and location
       - Severity: High
       - Note: Transient solve policy stored in canonical Model state

    4. **Pattern: Panic-based HiGHS construction**
       - `git grep -n 'panic\|expect\|unwrap' planning/roml-M1-native-backends-release -- roml-highs/src/adapter.rs | head -10`
       - Record whether `new()` or constructor panics
       - Severity: High
       - Note: Violates D-007 (fallible construction)

    5. **Pattern: Legacy `Change` type still wired**
       - `git grep -n 'pub enum Change\|pub struct Change' planning/roml-M1-native-backends-release -- src/`
       - `git grep -n 'Vec<Change>' planning/roml-M1-native-backends-release -- src/` — check drain_changes return type
       - Severity: Critical
       - Note: The Change-based path goes away with M1R-01

    Write a table:

    | Pattern | Location | Line | Severity | M1R Phase to Address |
    |---------|----------|------|----------|----------------------|
    | drain_changes | src/model/mod.rs | {actual line} | Critical | M1R-01 |
    | SolverAdapter trait | roml-highs/src/adapter.rs | {actual line} | Critical | M1R-01 |
    | Model.solver_options | src/model/mod.rs | {actual line} | High | M1R-01 |
    | Panic construction | roml-highs/src/adapter.rs | {actual line(s)} | High | M1R-02 |
    | Legacy Change type | src/model/mod.rs | {actual line(s)} | Critical | M1R-01 |
  </action>
  <acceptance_criteria>
    - All 5 legacy patterns documented with actual file:line references from the candidate branch
    - Each pattern has a severity rating (Critical or High)
    - Each pattern maps to the correct M1R phase for resolution
    - All line numbers verified via git grep against planning/roml-M1-native-backends-release
  </acceptance_criteria>
  <verify>
    <automated>grep -c '| Critical |' docs/release/evidence/M1R/artifacts/02-claim-reconciliation.md && grep -c '| High |' docs/release/evidence/M1R/artifacts/02-claim-reconciliation.md</automated>
  </verify>
  <done>All 5 legacy source patterns documented with verified file:line references, severity ratings, and M1R phase assignments in 02-claim-reconciliation.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code on candidate branch -> Truth verdict | The executor reads source code from the candidate branch via `git show`/`git grep` and writes truth verdicts. Verdict accuracy depends on correct interpretation of source patterns. |
| RESEARCH.md claim reconciliation -> Evidence artifact | The executor uses RESEARCH.md claim tables as a checklist but MUST verify each claim independently. Copying RESEARCH.md without verification would propagate stale claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|----------|----------|-------------|-----------------|
| T-09-02-01 | Spoofing | RESEARCH.md claim propagation | medium | mitigate | Every claim disposition in Task 1 is backed by a fresh git command, not copied from RESEARCH.md. The task action explicitly forbids copying RESEARCH.md verdicts without verification. |
| T-09-02-02 | Repudiation | Missing legacy pattern detection | low | mitigate | The executor greps for known patterns from RESEARCH.md. The admission report compilation (Plan 05) cross-references patterns against the Research section. |
| T-09-02-03 | Information Disclosure | Source code structure exposed in evidence | low | accept | Evidence artifact is in docs/release/evidence/ which is committed to the public repo. No credentials or secrets are disclosed. |
</threat_model>

<verification>
- [ ] docs/release/evidence/M1R/artifacts/02-claim-reconciliation.md exists
- [ ] Claim disposition table covers M1.0-M1.5 with correct STATE.md vocabulary
- [ ] Every disposition is backed by a git command, not RESEARCH.md copy
- [ ] All 5 legacy patterns documented with verified line numbers
- [ ] Pattern severities match RESEARCH.md (Critical/High)
- [ ] Each pattern maps to the correct M1R phase
</verification>

<success_criteria>
- Requirement disposition table with 20+ rows covering M1.0-M1.5 sub-claims, each backed by git-verified source evidence
- Legacy pattern documentation with exact file:line references from the candidate branch
- Verdicts use only STATE.md vocabulary (Accepted/Partially Satisfied/Failed/Locally Verified/Cannot Verify/Owner-Blocked)
</success_criteria>

<output>
docs/release/evidence/M1R/artifacts/02-claim-reconciliation.md
</output>
