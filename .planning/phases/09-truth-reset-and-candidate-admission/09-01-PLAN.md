---
phase: 09-truth-reset-and-candidate-admission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/release/evidence/M1R/artifacts/01-evidence-foundation.md
autonomous: true
requirements:
  - M1R-G1
  - M1R-G3
  - M1R-G5
must_haves:
  truths:
    - All 20 candidate commits inventoried with SHA (first 7 hex chars), commit type, description, and classification (implementation vs documentation vs contaminated)
    - License files LICENSE-MIT and LICENSE-APACHE confirmed present on candidate branch at commit c4db3e0
    - Cargo.toml license field recorded as "MIT OR Apache-2.0"
    - Crates.io names verified via public API (both return 404 Not Found = available)
    - cargo owner --list output recorded (success or auth-failure)
    - Evidence artifact 01-evidence-foundation.md written with exact commands and output
  artifacts:
    - docs/release/evidence/M1R/artifacts/01-evidence-foundation.md
  key_links:
    - Candidate SHA -> git log output -> 20-commit count matches RESEARCH.md
    - c4db3e0 -> git show LICENSE-MIT and LICENSE-APACHE -> OWNER-BLOCKED disposition
    - crates.io API 404 responses -> OWNER-BLOCKED (names available, reservation deferred)
---

<objective>
Produce the foundational evidence artifact: the canonical commit inventory, license file evidence, and crates.io ownership status for the inherited M1 candidate branch.

Purpose: Record the exact candidate state (commit list, head SHA, merge base, license intent, crates.io ownership) as a verified baseline that all subsequent M1R phases reference. Every claim in this artifact is backed by a git command or API response, not by RESEARCH.md claims.
Output: docs/release/evidence/M1R/artifacts/01-evidence-foundation.md
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DECISIONS.md
@.planning/TRACEABILITY.md
@.planning/phases/09-truth-reset-and-candidate-admission/09-RESEARCH.md
@.planning/phases/09-truth-reset-and-candidate-admission/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create evidence directory and record canonical commit inventory</name>
  <files>docs/release/evidence/M1R/artifacts/01-evidence-foundation.md</files>
  <read_first>
    - .planning/phases/09-truth-reset-and-candidate-admission/09-RESEARCH.md (sections: Candidate Commit Inventory, Sources)
    - .planning/TRACEABILITY.md (evidence directory convention)
  </read_first>
  <action>
    1. `mkdir -p docs/release/evidence/M1R/artifacts/`
    2. Run these git commands and capture all output for the evidence artifact:
       - `git rev-parse planning/roml-M1-native-backends-release` — record the candidate HEAD SHA
       - `git rev-parse docs/public-release-production-roadmap` — record the planning branch HEAD SHA
       - `git merge-base main planning/roml-M1-native-backends-release` — record merge base
       - `git log --oneline --decorate main..planning/roml-M1-native-backends-release` — record all 20 commits
       - `git log --oneline --name-only main..planning/roml-M1-native-backends-release` — record changed files per commit
    3. For each of the 20 commits, write a table row with: abbreviated SHA (7 hex chars), commit type extracted from conventional-commit prefix (feat/fix/docs), description, classification per RESEARCH.md (implementation-only, documentation-only, contaminated), and whether the commit touches .planning/ files.
    4. The table MUST contain exactly 20 rows. Cross-check against the 20-commit enumeration in RESEARCH.md. Flag any discrepancy.
    5. Write the task output as the first section of 01-evidence-foundation.md. Use a H2 heading `## Commit Inventory` followed by the table of 20 commits.
  </action>
  <acceptance_criteria>
    - Evidence artifact 01-evidence-foundation.md exists and has a `## Commit Inventory` section
    - The commit table contains exactly 20 rows
    - Each row has SHA, type, description, and classification
    - The HEAD SHA and merge base SHA are recorded
  </acceptance_criteria>
  <verify>
    <automated>grep -c '| \`[0-9a-f]\{7\}\`' docs/release/evidence/M1R/artifacts/01-evidence-foundation.md</automated>
  </verify>
  <done>All 20 commits are inventoried with SHA, type, description, classification, and contamination status in 01-evidence-foundation.md</done>
</task>

<task type="auto">
  <name>Task 2: Record license evidence and verify crates.io names</name>
  <files>docs/release/evidence/M1R/artifacts/01-evidence-foundation.md</files>
  <read_first>
    - 09-CONTEXT.md (D4: License authorization, D5: Crates.io name verification)
    - 09-RESEARCH.md (sections: License File Evidence, Crates.io Ownership Status)
  </read_first>
  <action>
    Append two H2 sections to 01-evidence-foundation.md:

    **Section 1: License Evidence (H2 `## License Evidence`)**
    1. `git show c4db3e0:LICENSE-MIT | head -3` and `git show c4db3e0:LICENSE-APACHE | head -3` to confirm the license files exist on the candidate branch. Record the file existence, copyright holder (Surya Krishnan, 2026), and commit SHA c4db3e0.
    2. `grep 'license' Cargo.toml` to confirm `license = "MIT OR Apache-2.0"` in workspace.package. Record the exact grep output.
    3. Write a table: | File | Commit SHA | Status | | LICENSE-MIT | c4db3e0 | Present | | LICENSE-APACHE | c4db3e0 | Present | | Cargo.toml license field | c4db3e0 | "MIT OR Apache-2.0" |
    4. Record the determination: **OWNER-BLOCKED** per D4 — files committed but explicit owner confirmation deferred to M1R-08.

    **Section 2: Crates.io Verification (H2 `## Crates.io Verification`)**
    1. `curl -s -o /dev/null -w "%{http_code}" https://crates.io/api/v1/crates/roml` and record HTTP response code. If 404, name is available. Parse the JSON body: `curl -s https://crates.io/api/v1/crates/roml`.
    2. Same for `roml-highs`: `curl -s -o /dev/null -w "%{http_code}" https://crates.io/api/v1/crates/roml-highs` and `curl -s https://crates.io/api/v1/crates/roml-highs`.
    3. `cargo owner --list roml 2>&1 || echo "CARGO_OWNER_FAILED: no auth token"` — record output. If the command fails with "no token found", record as "Auth not available — OWNER-BLOCKED".
    4. `cargo owner --list roml-highs 2>&1 || echo "CARGO_OWNER_FAILED: no auth token"` — same handling.
    5. Write a table: | Crate | API Response | cargo owner --list | Determination | | roml | 404 Not Found | Failed (no auth) | OWNER-BLOCKED | | roml-highs | 404 Not Found | Failed (no auth) | OWNER-BLOCKED |
    6. Per D5: if API returns 404 for both, determination is OWNER-BLOCKED (names available, reservation deferred per D-011). If either returns 200 (crate exists), escalate to EXTERNAL-BLOCKED with a program stop signal.
  </action>
  <acceptance_criteria>
    - License Evidence section documents LICENSE-MIT and LICENSE-APACHE at commit c4db3e0
    - Cargo.toml license field recorded as "MIT OR Apache-2.0"
    - Both crates.io API responses recorded (expected: 404 Not Found)
    - cargo owner --list output recorded for both crates
    - Ownership determination is OWNER-BLOCKED (unless API returns 200 for either crate, in which case escalate)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'OWNER-BLOCKED' docs/release/evidence/M1R/artifacts/01-evidence-foundation.md && grep -q 'LICENSE-MIT' docs/release/evidence/M1R/artifacts/01-evidence-foundation.md && grep -q 'c4db3e0' docs/release/evidence/M1R/artifacts/01-evidence-foundation.md</automated>
  </verify>
  <done>License evidence, Cargo.toml declaration, and crates.io ownership verification are recorded in 01-evidence-foundation.md with OWNER-BLOCKED disposition</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git log output -> Evidence artifact | The executor reads git commit history and writes it to the artifact. Git SHAs are tamper-evident; the executor cannot forge a git log entry. |
| crates.io API response -> Ownership determination | The executor trusts the HTTP response from crates.io API. A MITM or DNS hijack could return false data, but this is a governance artifact, not a cryptographic release gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|----------|----------|-------------|-----------------|
| T-09-01-01 | Spoofing | Evidence artifact authorship | low | mitigate | Every evidence entry cites exact source (commit SHA, command output, file:line). The M1R-00-ADMISSION.md compilation plan cross-references artifacts against each other. |
| T-09-01-02 | Repudiation | Missing commit from inventory | medium | mitigate | The executor runs `git log main..HEAD` programmatically; output is captured verbatim. The admission report auditor re-runs the same command and compares. |
| T-09-01-03 | Tampering | crates.io API response trust | low | accept | The curl API check is a read-only governance check, not a release gate. Crates.io names will be re-verified at M1R-08 with `cargo owner --list` and authenticated token. |
</threat_model>

<verification>
- [ ] docs/release/evidence/M1R/artifacts/01-evidence-foundation.md exists
- [ ] Commit inventory table has exactly 20 rows
- [ ] License files confirmed at commit c4db3e0
- [ ] Cargo.toml license field recorded
- [ ] Both crates.io API responses recorded
- [ ] cargo owner --list output recorded for both crates
- [ ] Determination documented as OWNER-BLOCKED
</verification>

<success_criteria>
- 01-evidence-foundation.md contains authoritative commit inventory (20 commits), license evidence, and crates.io ownership status
- OWNER-BLOCKED disposition documented for both license and crates.io
- Evidence is based on raw git and HTTP commands, not RESEARCH.md claims (though cross-checked for consistency)
</success_criteria>

<output>
docs/release/evidence/M1R/artifacts/01-evidence-foundation.md
</output>
