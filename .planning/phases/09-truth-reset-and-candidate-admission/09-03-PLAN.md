---
phase: 09-truth-reset-and-candidate-admission
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/release/evidence/M1R/artifacts/03-test-classification-contamination.md
autonomous: true
requirements:
  - M1R-G2
  - M1R-G3
  - M1R-G4
must_haves:
  truths:
    - All 11 ignored tests are listed with file:line, current behavior, reason-for-ignore string, fix category (fix-now vs pin-for-M1R-01), and requirement link
    - Each test's existence is confirmed against the candidate branch
    - Branch contamination is analyzed: 4 planning-only commits, 4 contaminated implementation commits, 3 stale-completion-claim commits identified
    - Clean PR branches are defined with exact cherry-pick commands and path exclusions
    - Evidence directory commitment (5d117cf, 8a1212f) documented for preservation in admission report
  artifacts:
    - docs/release/evidence/M1R/artifacts/03-test-classification-contamination.md
  key_links:
    - P1-1/P1-2 fix-now classification -> Plan 04 Task 1 action (remove #[ignore], update assertion)
    - P1-3/P1-4/P2-1 through P2-7 pin classification -> Plan 04 Tasks 2-3 action (re-mark with M1R-01 reason)
    - Contaminated commit c4db3e0 -> cherry-pick with --no-commit + exclude .planning/ -> clean feature branch
    - Split-and-replay strategy -> M1R-G4 lane separation requirement
---

<objective>
Classify all 11 ignored tests by fix category and analyze branch contamination for the split-and-replay strategy.

Purpose: Produce the per-test disposition table required by D2 and the clean-PR-branch plan required by D3. This evidence artifact directly drives Plan 04 (test fixes) and informs the admission report.
Output: docs/release/evidence/M1R/artifacts/03-test-classification-contamination.md
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-truth-reset-and-candidate-admission/09-RESEARCH.md (sections: Ignored Test Classification, Branch Contamination Analysis, Pitfalls)
@.planning/phases/09-truth-reset-and-candidate-admission/09-CONTEXT.md (D2: Ignored test disposition, D3: Branch contamination and replay strategy)
@.planning/REQUIREMENTS.md (M1R-G2, M1R-G4)
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify all 11 ignored tests with per-test disposition</name>
  <files>docs/release/evidence/M1R/artifacts/03-test-classification-contamination.md</files>
  <read_first>
    - 09-RESEARCH.md section "Ignored Test Classification" (full tables for P1 and P2 tests)
    - 09-CONTEXT.md D2 (fix categories: fix-now vs pin-for-M1R-01)
  </read_first>
  <action>
    1. Create the evidence artifact with `mkdir -p docs/release/evidence/M1R/artifacts/` then write to it.

    2. Confirm each ignored test exists on the candidate branch by running:
       `git grep -n '#\[ignore\]' planning/roml-M1-native-backends-release -- tests/`
       Record the output. Expected: 11 occurrences. If the count differs, enumerate all and flag the discrepancy.

    3. For each of the 11 tests, record a disposition row. Read each test's source via git show to confirm file:line, current #[ignore] annotation, and test body structure:

       **P1 tests (model_characterization.rs)** — 4 tests:
       - `git show planning/roml-M1-native-backends-release:tests/model_characterization.rs | sed -n '540,580p'` — read P1-1 and P1-2
       - `git show planning/roml-M1-native-backends-release:tests/model_characterization.rs | sed -n '810,870p'` — read P1-3 and P1-4

       **P2 tests (sync_characterization.rs)** — 7 tests:
       - `git show planning/roml-M1-native-backends-release:tests/sync_characterization.rs | sed -n '180,210p'` — P2-1
       - `git show planning/roml-M1-native-backends-release:tests/sync_characterization.rs | sed -n '218,250p'` — P2-2
       - `git show planning/roml-M1-native-backends-release:tests/sync_characterization.rs | sed -n '275,310p'` — P2-3
       - `git show planning/roml-M1-native-backends-release:tests/sync_characterization.rs | sed -n '308,340p'` — P2-4
       - `git show planning/roml-M1-native-backends-release:tests/sync_characterization.rs | sed -n '350,390p'` — P2-5
       - `git show planning/roml-M1-native-backends-release:tests/sync_characterization.rs | sed -n '405,440p'` — P2-6
       - `git show planning/roml-M1-native-backends-release:tests/sync_characterization.rs | sed -n '453,490p'` — P2-7

    4. Write a H2 `## Ignored Test Disposition Table` with these columns:

       | ID | Test Name | File | Line | Current Behavior | Ignore Reason | Fix Category | Fix Action | Requirement |
       |----|-----------|------|------|-----------------|---------------|--------------|------------|-------------|

       Populate using RESEARCH.md classification. For Fix Category use exactly one of:
       - **fix-now** — remove #[ignore], update expectation to match canonical behavior
       - **pin-m1r1** — update expectation to desired post-M1R-01 behavior, re-mark with `#[ignore = "resolved in M1R-01 — drain_changes removal"]`

       Fix Category assignments per D2:
       - P1-1, P1-2: fix-now
       - P1-3, P1-4: pin-m1r1
       - P2-1 through P2-7: pin-m1r1

       For Requirement, map to M1R-C or M1R-Q requirement ID per TRACEABILITY.md:
       - P1-1, P1-2: M1R-C5 (canonical cell behavior)
       - P1-3: M1R-H7 (semi-continuous recovery)
       - P1-4: M1R-C2 (solve policy outside Model)
       - P2-1 through P2-7: M1R-C1 (destructive drain removal), M1R-C4 (health model)

    5. Add a summary row: | **Total** | **11** | | | | | 2 fix-now + 9 pin-m1r1 | |

    6. Add a note: "IMPORTANT: Test files exist only on branch planning/roml-M1-native-backends-release, not on this planning branch. Plan 04 (wave 2) uses git worktree to check out the candidate branch, fix the tests, and commit to a feature branch."
  </action>
  <acceptance_criteria>
    - All 11 tests listed with file-existence confirmed via git grep against candidate branch
    - Each test has correct Fix Category (fix-now vs pin-m1r1) per D2
    - Each test maps to a M1R-C/M1R-Q requirement ID
    - Summary shows 2 fix-now + 9 pin-m1r1
    - A note about test files being on the candidate branch is included
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'fix-now' docs/release/evidence/M1R/artifacts/03-test-classification-contamination.md && grep -c 'pin-m1r1' docs/release/evidence/M1R/artifacts/03-test-classification-contamination.md</automated>
  </verify>
  <done>All 11 ignored tests classified: 2 fix-now (P1-1, P1-2) and 9 pin-m1r1 (P1-3, P1-4, P2-1 through P2-7), with per-test disposition table in 03-test-classification-contamination.md</done>
</task>

<task type="auto">
  <name>Task 2: Analyze branch contamination and define split-and-replay strategy</name>
  <files>docs/release/evidence/M1R/artifacts/03-test-classification-contamination.md</files>
  <read_first>
    - 09-RESEARCH.md section "Branch Contamination Analysis" (commit classification, planning-only commits, contaminated implementation commits, stale completion claims, split-and-replay strategy)
    - 09-RESEARCH.md section "Pitfall 4" (cherry-pick conflict resolution)
    - 09-CONTEXT.md D3 (Split and replay)
  </read_first>
  <action>
    Append H2 `## Branch Contamination Analysis` to 03-test-classification-contamination.md.

    Verify each commit classification by running:

    1. **Planning-only commits** (do NOT belong on implementation branches):
       ```
       git log --oneline --name-only planning/roml-M1-native-backends-release --not main -- .planning/
       ```
       Expected: 8 commits touch .planning/ files (4 planning-only + 4 contaminated implemntation). Record the list.

       ```
       git log --oneline planning/roml-M1-native-backends-release --not main -- .planning/ src/ tests/ roml-highs/ benches/
       ```
       Identify which commits touch ONLY .planning/ files. Those are planning-only. Expected 4: 073106f, bc8e3e0, dba71c0, 302b098.

    2. **Contaminated implementation commits** (touch both src/ AND .planning/):
       Cross-reference the .planning/-touching commits against the implementation commit list. Expected 4: c4db3e0, 22074c0, fb2cb88, 97f8792.

    3. **Stale completion claim commits** (3 commits claiming milestones complete):
       - a94b75e: "mark M1.2 complete"
       - 537a035: "mark M1.3 complete"
       - 85a6396: "mark M1.4 complete"
       Record these as planning-branch-only content.

    4. **Evidence directory commits** (belong in evidence, not on feature branches):
       - 5d117cf: semantic equivalence evidence tracker
       - 8a1212f: MOSEK qualification plan
       Record: preserve content in M1R-00-ADMISSION.md; do NOT cherry-pick to feature branches.

    5. Write a table with exact cherry-pick commands for each clean feature branch:

       | Feature Branch | Base | Commands |
       |---------------|------|----------|
       | feat/m1-1-backend-contract | main@ef37c88 | `git cherry-pick -n 22074c0 -- src/ tests/ roml-highs/ roml-mosek/ roml-xpress/ benches/` then reset .planning/ then commit |
       | feat/m1-2-highs-migration | main@ef37c88 | `git cherry-pick c1d5e90` |
       | feat/m1-3-status-tests | main@ef37c88 | `git cherry-pick c48f8c3 00a586c 084e58e` |
       | feat/m1-4-ci-harness | main@ef37c88 | `git cherry-pick -n fb2cb88 -- .github/ roml-highs/` then reset .planning/ then cherry-pick cf8dc8b 0fe295b |
       | feat/m1-5-benchmarks | main@ef37c88 | `git cherry-pick -n 97f8792 -- benches/ Cargo.toml` then reset .planning/ then commit |
       | feat/m1-6-mosek-xpress-clippy | main@ef37c88 | `git cherry-pick 649c635` |

       For the 4 contaminated commits (c4db3e0, 22074c0, fb2cb88, 97f8792), the cherry-pick must:
       - Use `git cherry-pick -n <SHA>` (no-commit mode)
       - Reset .planning/ paths: `git reset HEAD -- .planning/`
       - Only commit the src/ and non-planning changes
       - Write a clean commit message without stale completion claims

       For 22074c0, the commit message says "M1.1: backend contract freeze". This is a descriptive label, not a stale claim — keep it. For fb2cb88 ("M1.4-M1.8: CI workflow, platform qual, performance plan, release prep"), rewrite the message to remove the milestone claim.

       Write note: "WARNING: after cherry-pick, verify `git diff --stat main..feature-branch -- src/` matches equivalent files on the original candidate branch. Run `git difftool` or `diff -r` against candidate files if there's any conflict."

       Write note: "Merge base is 82e2ed95 per `git merge-base main planning/roml-M1-native-backends-release`. The RESEARCH.md references ef37c88 as documented merge base, but main has advanced. Use 82e2ed95 as the actual base for cherry-picks."

    6. Write a H2 `## Remaining Files on Planning Branch` section listing commits that stay on the planning branch: 073106f, bc8e3e0, dba71c0, 302b098, a94b75e, 537a035, 85a6396, 5d117cf, 8a1212f.
  </action>
  <acceptance_criteria>
    - Planning-only commits identified and confirmed via git log with path filter
    - Contaminated implementation commits listed with their dual-path nature
    - Stale completion claim commits identified with content preservation plan
    - 6 clean feature branches defined with exact git commands
    - Cherry-pick conflict resolution guidance included for contaminated commits
    - Actual merge base (82e2ed95) documented, along with discrepancy from researched base (ef37c88)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'feat/' docs/release/evidence/M1R/artifacts/03-test-classification-contamination.md && grep -c 'cherry-pick' docs/release/evidence/M1R/artifacts/03-test-classification-contamination.md</automated>
  </verify>
  <done>Branch contamination analyzed with 6 clean feature branches defined; exact cherry-pick commands documented in 03-test-classification-contamination.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test classification -> Fix strategy | Incorrect classification (e.g., labeling fix-now as pin-m1r1) would cause downstream correctness errors. The fix-action column encodes per-D2 disposition. |
| Cherry-pick commands -> Clean feature branches | Incorrect cherry-pick commands could drop or duplicate files, introducing subtle semantic drift into feature branches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|----------|----------|-------------|-----------------|
| T-09-03-01 | Tampering | Test classification labels | medium | mitigate | Task 1 verifies each test exists and records its current #[ignore] line from the candidate branch before assigning a fix category. The admission report cross-checks assignments against the research. |
| T-09-03-02 | Tampering | Cherry-pick command accuracy | medium | mitigate | Task 2 documents exact cherry-pick commands for each clean feature branch, including path-exclusion flags. A verification note instructs the implementer to diff against the original. |
| T-09-03-03 | Repudiation | Contaminated commit identification | low | mitigate | All identifications are backed by `git log --name-only` output, not by RESEARCH.md claims. |
</threat_model>

<verification>
- [ ] docs/release/evidence/M1R/artifacts/03-test-classification-contamination.md exists
- [ ] All 11 ignored tests listed with file:line, current behavior, fix category, requirement link
- [ ] Fix categories: 2 fix-now, 9 pin-m1r1
- [ ] 4 planning-only commits verified via git log
- [ ] 4 contaminated implementation commits identified
- [ ] 3 stale-completion-claim commits documented
- [ ] 6 clean feature branches with exact cherry-pick commands
- [ ] Actual merge base SHA (82e2ed95) recorded
</verification>

<success_criteria>
- Per-test disposition table for all 11 ignored tests with correct fix category and requirement mapping
- Branch contamination analysis with 6 clean feature branch definitions and exact git commands
- Planning-only, contaminated implementation, stale-claim, and evidence-directory commits all classified
</success_criteria>

<output>
docs/release/evidence/M1R/artifacts/03-test-classification-contamination.md
</output>
