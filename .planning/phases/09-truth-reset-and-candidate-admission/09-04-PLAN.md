---
phase: 09-truth-reset-and-candidate-admission
plan: 04
type: execute
wave: 2
depends_on:
  - 09-03
files_modified:
  - tests/model_characterization.rs
  - tests/sync_characterization.rs
  - docs/release/evidence/M1R/artifacts/04-test-fix-report.md
autonomous: true
requirements:
  - M1R-G2
must_haves:
  truths:
    - P1-1 (duplicate_coefficient_for_same_cell) no longer has #[ignore]; test assertion updated to expect 1 combined coefficient instead of 2
    - P1-2 (duplicate_coefficient_in_objective) no longer has #[ignore]; test assertion updated to expect 1 combined coefficient instead of 2
    - P1-3 (set_semicontinuous_low_lower_emits_change_without_bounds_update) updated and re-ignored with `#[ignore = "resolved in M1R-01 — drain_changes removal"]`
    - P1-4 (solve_options_stored_on_model_and_consumed_during_solve) updated and re-ignored with `#[ignore = "resolved in M1R-01 — solve policy removal from Model"]`
    - P2-1 through P2-7 in sync_characterization.rs updated and re-ignored with `#[ignore = "resolved in M1R-01 — drain_changes removal"]`
    - All changes committed to branch `fix/m1r-00-ignored-tests` from worktree
    - Evidence artifact 04-test-fix-report.md documents every change applied
  artifacts:
    - fix/m1r-00-ignored-tests (new branch from candidate branch)
    - docs/release/evidence/M1R/artifacts/04-test-fix-report.md
  key_links:
    - 03-test-classification-contamination.md fix-now assignments -> Task 1 edits (P1-1, P1-2)
    - 03-test-classification-contamination.md pin-m1r1 assignments -> Tasks 2 and 3 edits (P1-3, P1-4, P2-1 through P2-7)
    - test file changes committed to fix/m1r-00-ignored-tests -> verification by Plan 05
---

<objective>
Fix all 11 ignored tests on the candidate branch by updating test expectations to match canonical or desired post-M1R-01 behavior, then commit to a clean feature branch.

Purpose: M1R-G2 forbids ignored/skipped checks from satisfying gates. After this plan, 0 tests remain with stale #[ignore] annotations. The 2 fix-now tests become actively running assertions. The 9 pin-m1r1 tests document the gap that M1R-01 closes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-truth-reset-and-candidate-admission/09-03-PLAN.md ("Artifacts this phase produces" section)
@.planning/phases/09-truth-reset-and-candidate-admission/09-RESEARCH.md (Ignored Test Classification tables with exact line numbers)
@.planning/phases/09-truth-reset-and-candidate-admission/09-CONTEXT.md (D2: fix-now vs pin-m1r1 test disposition)
@.planning/REQUIREMENTS.md (M1R-G2)
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create worktree from candidate branch for test fix operations</name>
  <files/>
  <read_first>
    - 09-03-PLAN.md task 1 note about test files existing only on candidate branch
    - 09-RESEARCH.md section "Environment Availability"
  </read_first>
  <action>
    This task sets up the worktree that Tasks 1-3 operate within.

    The test files `tests/model_characterization.rs` and `tests/sync_characterization.rs` exist ONLY on branch planning/roml-M1-native-backends-release, not on the current planning branch. All test modifications must happen in a worktree.

    Steps:
    1. WORKTREE_DIR=$(mktemp -d /tmp/roml-m1r-test-fix-XXXXX)
    2. git worktree add "$WORKTREE_DIR" planning/roml-M1-native-backends-release
    3. Verify the worktree has the required test files: ls -la "$WORKTREE_DIR/tests/model_characterization.rs" "$WORKTREE_DIR/tests/sync_characterization.rs"
    4. cd "$WORKTREE_DIR"
    5. Create a fix branch from the current HEAD: git checkout -b fix/m1r-00-ignored-tests
    6. Record: echo "WORKTREE=$WORKTREE_DIR" > /tmp/roml-test-fix-state.sh
    7. Record: echo "FIX_BRANCH=fix/m1r-00-ignored-tests" >> /tmp/roml-test-fix-state.sh

    Keep the worktree active across Tasks 1-3. Tasks 1-3 operate on files inside $WORKTREE_DIR.
  </action>
  <acceptance_criteria>
    - Worktree created at temporary directory
    - Worktree points to planning/roml-M1-native-backends-release commit
    - Branch fix/m1r-00-ignored-tests created from that commit
    - Both test files confirmed present
    - State file /tmp/roml-test-fix-state.sh exists with WORKTREE and FIX_BRANCH vars
  </acceptance_criteria>
  <verify>
    <automated>ls /tmp/roml-test-fix-state.sh && grep FIX_BRANCH /tmp/roml-test-fix-state.sh</automated>
  </verify>
  <done>Worktree established and fix branch created for Task 1-3 modifications</done>
</task>

<task type="auto">
  <name>Task 1: Fix P1-1 and P1-2 — remove ignore, update to canonical-cell expectation</name>
  <files>tests/model_characterization.rs</files>
  <read_first>
    - 09-RESEARCH.md section "P1 Tests (4 ignored)" (P1-1, P1-2 entries showing current behavior and fix required)
    - (via worktree) $WORKTREE_DIR/tests/model_characterization.rs lines 540-580
    - 09-CONTEXT.md D2 (fix-now category)
  </read_first>
  <action>
    Source the state file: . /tmp/roml-test-fix-state.sh

    Read the current test source: sed -n '540,580p' "$WORKTREE_DIR/tests/model_characterization.rs"

    P1-1 ("duplicate_coefficient_for_same_cell") at approximately line 545:
    - Current behavior: Two coefficients for the same (constraint, variable) cell produce 2 entries in the change list. This is the legacy last-write-wins behavior.
    - Fix: The canonical cell implementation combines coefficients algebraically, so duplicate coefficients for the same cell result in 1 combined entry.
    - Action: Remove the #[ignore] annotation from the test. Update the test assertion expecting 2 entries to instead expect 1 combined coefficient. Keep the test name and general structure — only change the assertion value and remove #[ignore].

    P1-2 ("duplicate_coefficient_in_objective") at approximately line 566:
    - Same structural fix as P1-1 but for objective coefficients.
    - Action: Remove the #[ignore] annotation. Update the assertion to expect 1 combined objective coefficient instead of 2.

    After both edits:
    1. Verify the file compiles: cd "$WORKTREE_DIR" && cargo test -p roml duplicate_coefficient_for_same_cell -- --test-threads=1 2>&1 | tail -20
    2. Verify P1-2 also passes: cd "$WORKTREE_DIR" && cargo test -p roml duplicate_coefficient_in_objective -- --test-threads=1 2>&1 | tail -20
    3. Verify no bare #[ignore] remains in model_characterization.rs for the fixed tests: grep -n 'ignore' "$WORKTREE_DIR/tests/model_characterization.rs"
  </action>
  <acceptance_criteria>
    - P1-1 test no longer has #[ignore] annotation
    - P1-1 test assertion updated to expect 1 combined coefficient instead of 2
    - P1-2 test no longer has #[ignore] annotation
    - P1-2 test assertion updated to expect 1 combined coefficient instead of 2
    - cargo test -p roml duplicate_coefficient_for_same_cell passes
    - cargo test -p roml duplicate_coefficient_in_objective passes
  </acceptance_criteria>
  <verify>
    <automated>. /tmp/roml-test-fix-state.sh && cd "$WORKTREE_DIR" && cargo test -p roml duplicate_coefficient_for_same_cell duplicate_coefficient_in_objective -- --test-threads=1 2>&1 | grep -E "test result.*(FAILED|ok)"</automated>
  </verify>
  <done>P1-1 and P1-2 are fixed: #[ignore] removed, assertions updated for canonical cell behavior, both tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Pin P1-3 and P1-4 — update expectations and re-mark with M1R-01 ignore reason</name>
  <files>tests/model_characterization.rs</files>
  <read_first>
    - 09-RESEARCH.md section "P1 Tests (4 ignored)" (P1-3, P1-4 entries)
    - 09-CONTEXT.md D2 (pin-m1r1 category: "Record as #[ignore = 'resolved in M1R-01 — drain_changes removal']")
    - (via worktree) $WORKTREE_DIR/tests/model_characterization.rs lines 810-870
  </read_first>
  <action>
    Source the state file: . /tmp/roml-test-fix-state.sh

    Read the current test source: sed -n '810,870p' "$WORKTREE_DIR/tests/model_characterization.rs"

    P1-3 ("set_semicontinuous_low_lower_emits_change_without_bounds_update") at approximately line 818:
    - Current behavior: Setting semi-continuous lower to 3.0 when current lower is 5.0 emits a SemiContinuousBoundChanged change via drain_changes(). The bounds don't actually change (3.0 <= 5.0 is filtered), but the Change path emits anyway.
    - Fix: The Change-based emission path is being removed by M1R-01. The DeltaBatch path handles this correctly.
    - Action: Update the test assertions to reflect the desired post-M1R-01 behavior (document what should happen in the DeltaBatch/Cursor protocol). Change the ignore annotation from existing text to: `#[ignore = "resolved in M1R-01 — drain_changes removal"]`

    P1-4 ("solve_options_stored_on_model_and_consumed_during_solve") at approximately line 842:
    - Current behavior: SolveOptions set on Model, no public getter, consumed during solve.
    - Fix: The SolveRequest type exists but Model.solver_options field persists. M1R-01 will remove it.
    - Action: Update the test to assert via the SolveRequest path instead of Model.solver_options. Change the ignore annotation from existing text to: `#[ignore = "resolved in M1R-01 — solve policy removal from Model"]`

    After both edits:
    1. Verify the file compiles: cd "$WORKTREE_DIR" && cargo check -p roml --tests 2>&1 | tail -5
    2. Verify only 2 #[ignore] remain in model_characterization.rs: grep -c 'ignore' "$WORKTREE_DIR/tests/model_characterization.rs"
       (Should show exactly 2 — the two we just re-annotated. Task 1 removed 2.)
  </action>
  <acceptance_criteria>
    - P1-3 test assertion updated and re-ignored with the exact string: `#[ignore = "resolved in M1R-01 — drain_changes removal"]`
    - P1-4 test assertion updated and re-ignored with the exact string: `#[ignore = "resolved in M1R-01 — solve policy removal from Model"]`
    - cargo check -p roml --tests passes
    - model_characterization.rs has exactly 2 #[ignore] annotations remaining
  </acceptance_criteria>
  <verify>
    <automated>. /tmp/roml-test-fix-state.sh && grep -c 'resolved in M1R-01' "$WORKTREE_DIR/tests/model_characterization.rs"</automated>
  </verify>
  <done>P1-3 and P1-4 pinned with correct M1R-01 ignore annotations and updated expectations</done>
</task>

<task type="auto">
  <name>Task 3: Pin P2-1 through P2-7 — update expectations and re-mark with M1R-01 drain_changes removal annotation</name>
  <files>tests/sync_characterization.rs</files>
  <read_first>
    - 09-RESEARCH.md section "P2 Tests (7 ignored)" (all 7 entries)
    - 09-CONTEXT.md D2 (pin-m1r1 category for all P2 tests)
    - (via worktree) $WORKTREE_DIR/tests/sync_characterization.rs lines 180-490
  </read_first>
  <action>
    Source the state file: . /tmp/roml-test-fix-state.sh

    Read each of the 7 P2 test sections. For each test, update the assertions to document the desired post-M1R-01 behavior of the revisioned protocol (cursor-based, non-destructive, recovery-capable), then replace the existing #[ignore] annotation with: #[ignore = "resolved in M1R-01 — drain_changes removal"]

    Test locations (approximate lines from RESEARCH.md, verify with line numbers from the actual file):

    1. P2-1 "drained_changes_are_lost_on_adapter_error" (~line 185):
       - Current: drain_changes() is destructive; error after drain means changes lost forever
       - Post-M1R-01 expectation: journal retains batch, retry possible via cursor acknowledgement
       - Action: Update assertion to reflect that after M1R-01, changes survive adapter errors. Re-mark ignore.

    2. P2-2 "error_during_apply_loses_changes_from_model" (~line 223):
       - Same as P2-1 but end-to-end with FailingAfterAdapter
       - Action: Update assertion to reflect changes survive apply errors. Re-mark ignore.

    3. P2-3 "two_adapters_cannot_both_sync_same_changes" (~line 281):
       - Current: Single-consumer changelog — after one adapter syncs, nothing left for second
       - Post-M1R-01 expectation: SyncCoordinator supports independent cursors; both adapters receive same changes
       - Action: Update assertion to expect both adapters see the same changes. Re-mark ignore.

    4. P2-4 "sync_model_leaves_nothing_for_second_adapter" (~line 313):
       - Same pattern as P2-3
       - Action: Update assertion to reflect independent cursor behavior. Re-mark ignore.

    5. P2-5 "no_recovery_path_after_partial_apply" (~line 356):
       - Current: Partial apply leaves model in undefined state
       - Post-M1R-01 expectation: ApplyOutcome::RequiresRebuild provides recovery
       - Action: Update assertion to document RequiresRebuild recovery. Re-mark ignore.

    6. P2-6 "reset_has_no_revision_check" (~line 411):
       - Current: No one checks adapter vs model revision mismatch
       - Post-M1R-01 expectation: AdapterCursor tracks applied_revision
       - Action: Update assertion to document revision-aware behavior. Re-mark ignore.

    7. P2-7 "no_staleness_detection_after_mutation" (~line 459):
       - Current: After mutation, adapter has no way to know its state is stale
       - Post-M1R-01 expectation: cursor revision comparison detects staleness
       - Action: Update assertion to document staleness detection. Re-mark ignore.

    After all 7 edits:
    1. cargo check -p roml --tests 2>&1 | tail -5
    2. Verify all 7 tests have the new ignore annotation: grep -c 'resolved in M1R-01' "$WORKTREE_DIR/tests/sync_characterization.rs"
       (Should show exactly 7)
    3. Summarize: echo "Total ignore annotations in model_characterization.rs: $(grep -c '#\[ignore' "$WORKTREE_DIR/tests/model_characterization.rs") (expected: 2)"
       echo "Total ignore annotations in sync_characterization.rs: $(grep -c '#\[ignore' "$WORKTREE_DIR/tests/sync_characterization.rs") (expected: 7)"
       echo "Total ignored tests remaining: $((2 + 7)) (expected: 9 — all with M1R-01 annotations)"
  </action>
  <acceptance_criteria>
    - All 7 P2 tests have updated assertions documenting desired post-M1R-01 revisioned protocol behavior
    - All 7 P2 tests have #[ignore] annotation updated to: `#[ignore = "resolved in M1R-01 — drain_changes removal"]`
    - cargo check -p roml --tests passes
    - sync_characterization.rs has exactly 7 #[ignore] annotations, each with the M1R-01 reason
  </acceptance_criteria>
  <verify>
    <automated>. /tmp/roml-test-fix-state.sh && CHECK_COUNT=$(grep -c 'resolved in M1R-01' "$WORKTREE_DIR/tests/sync_characterization.rs") && test "$CHECK_COUNT" = "7" && echo "OK: 7/7 P2 tests pinned"</automated>
  </verify>
  <done>All 7 P2 tests pinned with correct M1R-01 drain_changes removal annotation and updated expectations</done>
</task>

<task type="auto">
  <name>Task 4: Commit test fixes to feature branch, write evidence artifact, clean up worktree</name>
  <files>
    docs/release/evidence/M1R/artifacts/04-test-fix-report.md
  </files>
  <read_first>
    - 09-03-PLAN.md task 2 (branch contamination) for commit message conventions
  </read_first>
  <action>
    1. Commit changes to the fix branch in the worktree:
       . /tmp/roml-test-fix-state.sh
       cd "$WORKTREE_DIR"
       git add tests/model_characterization.rs tests/sync_characterization.rs
       git commit -m "fix(M1R-00): resolve all 11 ignored tests per D2 — 2 fix-now, 9 pin-for-M1R-01

       P1-1/P1-2: remove #[ignore], update expectations for canonical cell
       behavior (duplicate coefficients combine algebraically).

       P1-3/P1-4: update expectations, re-ignore with M1R-01 resolution
       annotations (drain_changes removal / solve policy removal).

       P2-1 through P2-7: update expectations for revisioned cursor protocol,
       re-ignore with M1R-01 drain_changes removal annotation.

       Requirements: M1R-G2
       Evidence: 09-03-test-classification-contamination.md"
       Record the commit SHA: COMMIT_SHA=$(git rev-parse HEAD); echo "FIX_COMMIT=$COMMIT_SHA" >> /tmp/roml-test-fix-state.sh

    2. Push the fix branch:
       git push origin fix/m1r-00-ignored-tests 2>&1 || echo "Push failed (no remote access) — branch exists locally"

    3. Return to the planning branch work directory:
       cd /Users/skrishnan/repos/roml

    4. Write evidence artifact to planning branch:
       mkdir -p docs/release/evidence/M1R/artifacts/
       Write docs/release/evidence/M1R/artifacts/04-test-fix-report.md with:
       - H2: Test Fix Report
       - The fix commit SHA
       - Table of all 11 tests with columns: ID, File, Fix Category, Status
       - Status column: "Fixed" for P1-1/P1-2, "Pinned (M1R-01)" for the 9 others
       - Note: "Branch fix/m1r-00-ignored-tests contains the committed changes"
       - Note: "Zero stale #[ignore] annotations remain. 2 active tests (P1-1, P1-2). 9 pinned tests with M1R-01 resolution annotations."

    5. Clean up the worktree:
       WORKTREE_DIR=$(grep WORKTREE /tmp/roml-test-fix-state.sh | cut -d= -f2)
       git worktree remove "$WORKTREE_DIR" 2>&1
       rm -f /tmp/roml-test-fix-state.sh
  </action>
  <acceptance_criteria>
    - Commit made on fix/m1r-00-ignored-tests with a descriptive message
    - Commit SHA recorded in /tmp/roml-test-fix-state.sh
    - Evidence artifact 04-test-fix-report.md exists on planning branch
    - Worktree cleaned up
    - All changes documented per test
  </acceptance_criteria>
  <verify>
    <automated>ls docs/release/evidence/M1R/artifacts/04-test-fix-report.md && git branch --list fix/m1r-00-ignored-tests | grep -q fix/m1r-00-ignored-tests</automated>
  </verify>
  <done>Test fixes committed to fix/m1r-00-ignored-tests, evidence artifact written on planning branch, worktree cleaned up</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file modifications on candidate branch | The worktree operates on the candidate branch. Changes are committed to a new feature branch (fix/m1r-00-ignored-tests), not directly to the candidate. |
| Test assertion updates | Incorrect assertion updates could silence real semantic bugs. The fix-now tests (P1-1, P1-2) match canonical cell behavior verified in the codebase. The pin-m1r1 tests document desired post-M1R-01 behavior explicitly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|----------|----------|-------------|-----------------|
| T-09-04-01 | Tampering | Fix-now assertion values | medium | mitigate | Task 1 explicitly states the new assertion expected value (1 combined coefficient instead of 2). The cargo test verify confirms the fix is correct. |
| T-09-04-02 | Tampering | Pin-m1r1 expectations drift | low | accept | Pinned tests remain #[ignore] and only document desired behavior. M1R-01 will remove the ignore and verify against the revisioned protocol. The annotation text binds the purpose. |
| T-09-04-03 | Repudiation | Evidence artifact missing edit details | low | mitigate | Task 4 writes a detailed report with each test's ID, file, fix category, and status. The commit message also enumerates all changes. |
</threat_model>

<verification>
- [ ] Tests in worktree compile: cargo check -p roml --tests passes
- [ ] P1-1, P1-2: #[ignore] removed, tests pass
- [ ] P1-3, P1-4: updated annotations match "resolved in M1R-01" pattern
- [ ] P2-1 through P2-7: updated annotations match "resolved in M1R-01" pattern
- [ ] Commit made on fix/m1r-00-ignored-tests
- [ ] 04-test-fix-report.md written with per-test disposition table
- [ ] Worktree cleaned up
</verification>

<success_criteria>
- 11 tests fully resolved: 2 fix-now (passing), 9 pin-m1r1 (documenting M1R-01 gap)
- Zero stale #[ignore] annotations
- Fix branch fix/m1r-00-ignored-tests exists with committed changes
- Evidence artifact 04-test-fix-report.md documents all changes
</success_criteria>

<output>
- fix/m1r-00-ignored-tests (branch)
- docs/release/evidence/M1R/artifacts/04-test-fix-report.md
</output>

