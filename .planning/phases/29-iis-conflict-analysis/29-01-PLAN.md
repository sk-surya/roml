---
phase: 29-iis-conflict-analysis
plan: 01
type: execute
wave: 1
depends_on: []
preconditions: canonical Phase 29 roadmap/path identity is resolved before execution
files_modified:
  - src/solver/infeasibility.rs
  - src/solver/facade.rs
  - src/solver/session.rs
  - src/solver/mod.rs
  - src/lib.rs
  - src/advanced.rs
  - tests/iis_contract.rs
  - tests/compiler_identity.rs
  - tests/solver_facade.rs
  - docs/release/evidence/P29_CONTRACT_CHARACTERIZATION.md
---

# Slice 1 — Contract and characterization

**Goal:** freeze the additive LP IIS vocabulary and record current compiler/session behavior before implementing reduction or native integration.

**Requirements/decisions:** packet D1–D4, D17–D19, D28; R2.9, R3.5, R4.1–R4.3, R4.6, R9.1–R9.3.

## Files and interfaces

- Modify `src/solver/infeasibility.rs`: replace the partial contract with `InfeasibilityMode`, `InfeasibilityScope`, `ConflictGrouping`, `SeedPolicy`, `ReductionPolicy`, `AnalysisBudget`, `AnalysisNumericalPolicy`, `FeasibilityOutcome`, `UnknownReason`, `InfeasibilityError`, and report/provider type declarations. Keep the oracle limited to the three outcome variants.
- Modify `src/solver/facade.rs`: add the public `SolverSession::analyze_infeasibility(&mut self, &Model, &InfeasibilityPlan) -> Result<InfeasibilityReport, InfeasibilityError>` seam. Its interim implementation must return typed unsupported without mutating `self`, `model`, or the persistent backend.
- Modify `src/solver/session.rs`: add the optional `InfeasibilityOracleFactory` capability boundary with a default typed unsupported operation; do not make all backends implement native IIS.
- Modify `src/solver/mod.rs`, `src/lib.rs`, and `src/advanced.rs`: export only the approved plan/mode/oracle/report names, with implementation stores private.
- Create `tests/iis_contract.rs`: public API and guarantee tests. Extend `tests/compiler_identity.rs` and `tests/solver_facade.rs` for exact snapshot/identity and persistent-session characterization.
- Create `docs/release/evidence/P29_CONTRACT_CHARACTERIZATION.md`: record base SHA, current source status, commands, RED tests, interfaces, and unresolved seams before implementation.

## Test-first execution

- [ ] **1. Record the baseline and routing gate.** Run `git status --short --branch`, `git rev-parse HEAD`, and the core baseline commands from `AGENTS.md`. Record that the GSD resolver currently sees an absent roadmap Phase 29 plus two `29-*` directories; do not delete or rename the empty directory in this slice.
- [ ] **2. Write failing API-shape tests.** In `tests/iis_contract.rs`, assert that a `SolverSession` has the plan-driven entry point, `Model` has no IIS method in the intended public compile surface, the four modes are distinct, and `InfeasibilityScope::LpRelaxation` is explicit. Compile the test before adding the declarations and record the expected failure.
- [ ] **3. Write failing status/guarantee tests.** Cover every `TerminationStatus` variant: only proven LP feasible/infeasible statuses map to the two proven outcomes; ambiguous, unbounded, numerical, interrupted, limit, error, and unknown statuses map to `Unknown`. Assert that incomplete completion cannot produce `Irreducible` and that no public type contains a minimum-cardinality guarantee.
- [ ] **4. Write failing lineage tests.** Use the reference backend to characterize exact `ModelInstanceId`, `ModelRevision`, `CompilationId`, and the persistent backend revision before/after an unsupported analysis call. Assert no model revision, backend compilation, cursor, or solve result changes.
- [ ] **5. Implement the minimum contract.** Add the typed declarations and default-unsupported factory; do not add a reducer, native call, or model mutation. Make `CompilationId` mandatory in the planned report rather than carrying forward the partial `Option<CompilationId>` field.
- [ ] **6. Run focused verification.** Run `cargo test -p roml --test iis_contract --test compiler_identity --test solver_facade`, then formatting, clippy, rustdoc, and `cargo package --list -p roml`. Update the evidence with actual output and skipped native checks.

## Acceptance and stop conditions

The slice passes only when the four modes, LP scope labels, tri-state status rule, typed unsupported behavior, exact identity fields, and persistent-session no-op behavior are executable. Stop before Slice 2 if the existing P28 session/facade contract cannot expose an isolated-analysis seam without mutating the persistent session or if the canonical `CompilationId` semantics are not exact.

**Commit boundary:** `feat(iis): freeze LP infeasibility contract` after the focused checks and evidence update.
