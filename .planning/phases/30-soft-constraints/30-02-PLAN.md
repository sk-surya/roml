---
phase: 30-soft-constraints
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - src/construct/soft_constraint.rs
  - src/model/mod.rs
  - src/compiler/bridge/mod.rs
  - src/compiler/bridge/soft_constraint.rs
  - src/compiler/origin.rs
  - src/compiler/session.rs
  - src/compiler/mod.rs
  - src/solution/mod.rs
  - tests/soft_constraints_algebra.rs
  - tests/soft_constraints_origins.rs
  - tests/soft_constraint_solution.rs
  - tests/support/soft_constraints_reference.rs
  - roml-highs/tests/soft_constraints_differential.rs
autonomous: true
requirements: [SM-10.2, SM-10.3, SM-10.4, SM-10.5, SM-10.6, SM-10.7, SM-10.8]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Upper, lower, equality, and ranged constraints compile to the exact signed side algebra with distinct lower/upper violations where both sides exist."
    - "Every generated violation variable and row has a stable construct role and complete origin; no compiler-generated member is orphaned."
    - "Finite nonnegative caps and parameterized penalty weights are validated/evaluated before compilation or backend mutation."
    - "Penalty sign handling is correct for minimization and maximization while P30 adds only `PenaltyTarget::None` and `PenaltyTarget::Objective`."
    - "Solutions expose raw lower, raw upper, and total violation values in original constraint terms; signed correction remains a separate explicit API."
  artifacts:
    - path: "src/compiler/bridge/soft_constraint.rs"
      provides: "Exact persistent soft-constraint compiler bridge and independent violation-role allocation."
    - path: "src/compiler/origin.rs"
      provides: "Generated roles for lower/upper violation variables and rows."
    - path: "tests/support/soft_constraints_reference.rs"
      provides: "Test-local algebra oracle independent from the production bridge."
    - path: "src/solution/mod.rs"
      provides: "Original-constraint violation accessors and explicit signed-correction result access."
    - path: "roml-highs/tests/soft_constraints_differential.rs"
      provides: "Standard compiled-row ReferenceBackend/HiGHS differential qualification without native relaxation APIs."
  key_links:
    - from: "SoftConstraint payload"
      to: "soft_constraint bridge"
      via: "compiler dispatch receives the canonical payload and exact snapshot/parameter context"
    - from: "BridgeFinalizer"
      to: "OriginMap"
      via: "each generated variable/row is recorded with a role tied to the originating construct"
    - from: "evaluated penalty weights"
      to: "compiled objective/repair metadata"
      via: "finite values are captured at one model revision before any mutation"
    - from: "Solution"
      to: "original Constraint"
      via: "violation accessors aggregate generated lower/upper roles rather than exposing only backend IDs"
---

<objective>
Compile persistent soft constraints into exact portable linear rows, preserve generated provenance, and expose mathematically honest penalty and violation semantics.

Purpose: This wave proves the algebra and numeric boundary before the solve-scoped repair executor is allowed to consume the construct.
Output: compiler bridge, role/origin integration, weight evaluation, signed correction, violation accessors, independent reference tests, and standard HiGHS differential coverage.
</objective>

<execution_context>
@/home/skrishnan/.codex/gsd-core/workflows/execute-plan.md
@/home/skrishnan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/M3-semantic-modeling-workflows/SHARED-CONTRACTS.md
@.planning/phases/30-soft-constraints/30-CONTEXT.md
@.planning/phases/30-soft-constraints/30-PLAN.md
@.planning/phases/30-soft-constraints/30-01-PLAN.md
@src/compiler/bridge/mod.rs
@src/compiler/bridge/absolute.rs
@src/compiler/session.rs
@src/compiler/origin.rs
@src/solution/mod.rs

<interfaces>
Use `BridgeContext`, `BridgeFinalizer`, `BridgeOutput`, `GeneratedRole`, `EntityOrigin`, `CompilationSession::compile_snapshot`, and `ModelSnapshot` as the existing compiler seams. `ConstraintBounds` stores finite lower/upper sides with infinities for absent sides. `ValueExpr::eval_checked` is the numeric-validation pattern. The HiGHS test must consume compiled rows through the existing safe `HighsSession`, not raw native relaxation calls.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 30-02: Wire one exact upper-side soft constraint through compilation and a solver</name>
  <files>src/compiler/bridge/mod.rs, src/compiler/bridge/soft_constraint.rs, src/compiler/origin.rs, src/compiler/session.rs, tests/soft_constraints_algebra.rs, tests/support/soft_constraints_reference.rs, roml-highs/tests/soft_constraints_differential.rs</files>
  <behavior>
    - Test 1: `f(x) <= u` compiles as `f(x) - v_up <= u` with `v_up >= 0`, and the generated variable/row resolve back to the original soft construct role.
    - Test 2: a reference projection and a compiled ReferenceBackend/HiGHS solve agree on feasibility and objective for a one-row violation fixture.
  </behavior>
  <action>Per D-02, D-03, D-11, and D-15, add the soft-constraint bridge to the existing bridge registry and compiler dispatch. Implement the single upper-side path completely: allocate a nonnegative generated violation variable with the canonical role, emit the exact row, record dependencies and origin entries through `BridgeFinalizer`, carry the cap when present, and surface typed compile errors for non-finite evaluated inputs or impossible bounds. Build the test-local reference formulation from the mathematical equation rather than importing bridge helpers, then run the same fixture through ReferenceBackend and the existing safe HiGHS compiled-row path. This tracer must be production-quality and become the implementation skeleton for the other sides; it must not add a native feasibility-relaxation API.
  <verify>
    <automated>cargo test -p roml --test soft_constraints_algebra --test soft_constraints_origins && cargo test -p roml-highs --test soft_constraints_differential</automated>
  </verify>
  <done>The upper-side happy path compiles, solves, preserves exact algebra, and has complete generated origins in both portable and standard HiGHS projection tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 30-03: Complete side algebra, caps, parameterized weights, and objective targeting</name>
  <files>src/construct/soft_constraint.rs, src/model/mod.rs, src/compiler/bridge/soft_constraint.rs, src/compiler/session.rs, src/compiler/mod.rs, tests/soft_constraints_algebra.rs, tests/soft_constraints_origins.rs, tests/support/soft_constraints_reference.rs</files>
  <behavior>
    - Test 1: lower, equality, and ranged constraints use `f(x) + v_lo >= l` and distinct upper/lower violations exactly as specified.
    - Test 2: finite cap zero, positive caps, negative cap, NaN, infinity, and parameter-evaluated caps follow the typed validation contract atomically.
    - Test 3: zero, positive, parameterized, minimization, and maximization penalty weights produce the expected normalized objective contribution and reject negative/nonfinite/unbound/stale values before mutation.
  </behavior>
  <action>Per D-02, D-03, D-06, D-11, and D-13, finish all four row-sense formulations in deterministic role order, preserve equality/ranged two-sided identity, and validate generated variable bounds against the finite cap. Resolve every `ValueExpr` weight and cap from one exact `(ModelInstanceId, ModelRevision)` snapshot before compiler/backend mutation; record evaluated weights and the selected `None`/`Objective` target in the solver-neutral report metadata. Normalize objective-sense signs using the existing objective semantics, and keep the P31-owned priority/objective-policy machinery untouched. Extend compiler errors and preflight checks so invalid values cannot become rows or objective cells.
  <verify>
    <automated>cargo test -p roml --test soft_constraints_algebra --test soft_constraints_origins -- --nocapture</automated>
  </verify>
  <done>All row senses, caps, weights, and the two P30 penalty targets match the independent reference equations with typed pre-mutation failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 30-04: Add original-constraint violation accessors and explicit signed correction</name>
  <files>src/construct/soft_constraint.rs, src/model/mod.rs, src/solution/mod.rs, tests/soft_constraint_solution.rs, tests/soft_constraints_algebra.rs</files>
  <behavior>
    - Test 1: a solution reports lower, upper, and total raw violations by original constraint and soft handle, including equality/ranged two-sided values.
    - Test 2: tolerance-adjusted presentation is distinct from raw violation values and cannot change the mathematical report.
    - Test 3: signed correction creates explicit positive/negative parts and cannot be inferred from persistent soft violation values.
  </behavior>
  <action>Per D-04 and SM-10.8, expose solution-facing accessors that resolve the original constraint and stable violation roles, return raw lower/upper/total magnitudes, and preserve the existing feasibility-tolerance presentation as a separate view. Add the signed-correction builder/result as a distinct explicit model operation with positive and negative parts, finite validation, penalty semantics, and origins; do not reuse or infer it from `SoftConstraint` slacks. Ensure stale solution compilation identities and missing role mappings return typed errors instead of fabricated zero values.
  <verify>
    <automated>cargo test -p roml --test soft_constraint_solution --test soft_constraints_algebra -- --nocapture</automated>
  </verify>
  <done>Users can inspect original lower/upper/total violations and request signed correction explicitly; no accessor or correction path conflates persistent softening with a solve overlay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical semantic state → compiler | Parameter expressions, caps, and constraint sides become backend IR. |
| Compiler IR → backend | Generated rows/variables and objective terms cross into ReferenceBackend/HiGHS. |
| Backend solution → public accessors | Solver values are mapped back through exact compilation/origin identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-04 | Tampering | soft-constraint bridge | high | mitigate | Independent equations, exact side tests, finite-cap checks, and generated-origin completeness validation. |
| T-30-05 | Information disclosure | evaluated penalty metadata | medium | mitigate | Record only mathematical values/identities; never include native credentials, paths, or raw FFI state. |
| T-30-06 | Repudiation | violation report | medium | mitigate | Preserve model instance/revision/compilation identity and original constraint provenance. |
| T-30-SC | Tampering | package/dependency installation | high | mitigate | No new package install; phase qualification runs the repository policy/slop checks and blocks on unreviewed package changes. |
</threat_model>

<verification>
<automated>cargo fmt --all -- --check && cargo test -p roml --test soft_constraints_algebra --test soft_constraints_origins --test soft_constraint_solution</automated>
</verification>

<success_criteria>
Persistent softening has independently proven exact algebra, complete origins, validated caps/weights, correct objective sense, explicit signed correction, and original-constraint violation reporting.
</success_criteria>

<output>
Create `.planning/phases/30-soft-constraints/30-02-SUMMARY.md` when done.
</output>
