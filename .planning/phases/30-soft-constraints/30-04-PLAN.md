---
phase: 30-soft-constraints
plan: 04
type: execute
wave: 4
depends_on: [30-03]
files_modified:
  - src/solver/relaxation.rs
  - src/solver/infeasibility.rs
  - tests/feasibility_relaxation_p29.rs
  - tests/soft_constraints_qualification.rs
  - docs/examples/feasibility_relaxation.md
  - docs/release/evidence/P30_SOFT_CONSTRAINTS_RELAXATION.md
  - CHANGELOG.md
autonomous: true
requirements: [SM-10.6, SM-10.9]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Supported P29 IIS origins convert to relaxation restrictions all-or-error, retaining imported source provenance."
    - "Temporary locks, overlay-only fixings, grouped constructs, compiler-only members, and stale instance/revision/compilation identities reject before backend mutation."
    - "Qualification evidence covers every P30-owned SM-10 leaf, both acceptance policies, parameterized weights, all row senses/caps, P29 supported/unsupported origins, and the complete fault matrix."
    - "Public documentation distinguishes persistent softening from solve-scoped repair and does not claim minimum-cardinality/minimum-weight repair or native relaxation support beyond evidence."
    - "The exact-head closure record includes commands, versions, skipped native-relaxation work, review disposition, residual risks, and owner-merge gate without publishing or activating P31."
  artifacts:
    - path: "src/solver/relaxation.rs"
      provides: "All-or-error conversion from supported P29 conflict members to relaxation restrictions and source provenance."
    - path: "tests/feasibility_relaxation_p29.rs"
      provides: "Imported MPS, primitive, bound, fixing, unsupported-origin, and stale-identity composition corpus."
    - path: "tests/soft_constraints_qualification.rs"
      provides: "P30 closure corpus and machine-checkable positive predicate inputs."
    - path: "docs/release/evidence/P30_SOFT_CONSTRAINTS_RELAXATION.md"
      provides: "Exact-head P30 requirement ledger, commands, outcomes, fault evidence, review, and residuals."
    - path: "docs/examples/feasibility_relaxation.md"
      provides: "Public solver-facing example showing persistent softening versus isolated portable repair."
  key_links:
    - from: "P29 ConflictMember / ConflictOrigin"
      to: "RelaxationRestriction"
      via: "explicit supported-origin conversion validates exact model/revision/compilation identity"
    - from: "imported MPS provenance"
      to: "RelaxationReport members"
      via: "source row/bound metadata survives conversion and reporting"
    - from: "qualification corpus"
      to: "P30 evidence ledger"
      via: "each SM-10 leaf maps to a named test and exact command result"
---

<objective>
Complete P29 composition and the exact-head P30 qualification/evidence gate for persistent softening and portable feasibility relaxation.

Purpose: D-09, D-10, D-12, and the P30 gate require semantic provenance and guarantee discipline, not merely a solver result.
Output: all-or-error P29 mapping, imported-source fixtures, qualification corpus, public example, changelog entry, and exact-head evidence.
</objective>

<execution_context>
@/home/skrishnan/.codex/gsd-core/workflows/execute-plan.md
@/home/skrishnan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/M3-semantic-modeling-workflows/SHARED-CONTRACTS.md
@.planning/milestones/M3-semantic-modeling-workflows/COMPLETION-ROADMAP.md
@.planning/phases/30-soft-constraints/30-CONTEXT.md
@.planning/phases/30-soft-constraints/30-PLAN.md
@.planning/phases/29-iis-conflict-analysis/29-CONTEXT.md
@.planning/phases/35-mps-import/35-CONTEXT.md
@src/solver/infeasibility.rs
@src/solver/relaxation.rs

<interfaces>
Use P29's `ConflictMember`, `ConflictOrigin`, restriction universe, exact `ModelInstanceId`/`ModelRevision`/`CompilationId` fields, and P35's imported source-aware row/bound origins. The conversion must return one complete restriction set or one typed error; it must not filter unsupported members and continue.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 30-06: Wire one supported P29 origin through all-or-error mapping and repair reporting</name>
  <files>src/solver/relaxation.rs, src/solver/infeasibility.rs, tests/feasibility_relaxation_p29.rs</files>
  <behavior>
    - Test 1: a P29 primitive upper-side conflict member maps to one `ConstraintSide` restriction and appears in the repair report with matching source identity.
    - Test 2: imported MPS row-side provenance survives the map and is visible in the resulting restriction/report member.
    - Test 3: one unsupported member causes the complete map to reject without invoking synchronization or backend mutation.
  </behavior>
  <action>Per D-09, D-10, D-12, and D-15, add the explicit P29-to-P30 conversion seam beside the existing restriction/origin types. Accept only primitive/imported constraint sides, declared/imported variable-bound sides, and persistent fixing atoms; require exact model instance, revision, and compilation identity before conversion. Preserve imported source provenance in the mapped restriction/report, reject temporary locks, overlay-only fixings, grouped constructs, compiler-only members, and stale identities with typed errors, and make the conversion transactional all-or-error. Use IIS only to select/diagnose eligible restrictions; do not attach a minimum-repair guarantee.
  <verify>
    <automated>cargo test -p roml --test feasibility_relaxation_p29 -- --nocapture</automated>
  </verify>
  <done>A supported P29 member reaches a source-aware portable repair report, while every unsupported/stale member rejects the complete conversion before mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 30-08A: Run the complete P30 qualification corpus and positive closure checks</name>
  <files>tests/soft_constraints_qualification.rs, tests/feasibility_relaxation_p29.rs, docs/release/evidence/P30_SOFT_CONSTRAINTS_RELAXATION.md</files>
  <behavior>
    - Test 1: every upper/lower/equality/ranged fixture covers finite caps, zero caps, zero/positive/parameterized weights, and both objective senses.
    - Test 2: both `RequireOptimal` and `AcceptFeasible` acceptance policies produce the frozen outcome distinctions, including `Unknown` for unproven feasibility under `RequireOptimal`.
    - Test 3: the P29 supported/unsupported origin corpus and fault matrix produce explicit pass/fail rows with no classified skip hiding a failure.
  </behavior>
  <action>Per D-07, D-08, D-11, and M3-C02–M3-C04, assemble the deterministic P30 corpus from the independent algebra/reference fixtures, portable repair tests, P29 composition cases, and fault-injection harness. Write one evidence row for each P30-owned SM-10 leaf and each required gate condition, including exact candidate SHA, toolchain, backend/version scope, commands, outputs, review disposition, and residual risk. The closure runner must fail on missing cases, unsupported claims, lost cleanup errors, fabricated numeric fields, or unresolved identity mismatches; native relaxation availability is recorded as an explicit non-required scope fence rather than a pass.
  <verify>
    <automated>cargo test -p roml --test soft_constraints_qualification --test feasibility_relaxation_p29 -- --nocapture</automated>
  </verify>
  <done>The P30 qualification corpus has explicit evidence for every owned leaf and gate, with no silent skips or guarantee inflation.</done>
</task>

<task type="auto">
  <name>Task 30-08B: Publish the P30 example, behavior documentation, and closure handoff</name>
  <files>docs/examples/feasibility_relaxation.md, CHANGELOG.md, docs/release/evidence/P30_SOFT_CONSTRAINTS_RELAXATION.md</files>
  <action>Per D-06, D-08, D-12, D-13, and D-14, document one runnable example that first creates persistent canonical softening and then performs an isolated portable weighted-L1 repair, showing defaults, explicit restrictions, outcome/metadata inspection, and unchanged canonical revision. State that IIS membership is diagnostic scope only, that P31 owns priority/objective-policy execution, and that this phase does not claim native feasibility-relaxation execution. Add the user-visible behavior to `CHANGELOG.md`, run the complete core/HiGHS formatting, lint, test, rustdoc, package, policy, and exact-head review commands required by `AGENTS.md`, and record skipped native-license checks without presenting them as passes. Do not publish crates, create tags, modify `.planning/STATE.md` to activate P31, or perform release actions.
  <verify>
    <automated>cargo fmt --all -- --check && cargo test -p roml --all-targets && cargo test -p roml-highs --all-targets && cargo clippy -p roml --all-targets -- -D warnings && cargo clippy -p roml-highs --all-targets -- -D warnings && RUSTDOCFLAGS='-D warnings' cargo doc -p roml -p roml-highs --no-deps && cargo package --list -p roml</automated>
  </verify>
  <done>Documentation and changelog match the implemented portable contract; exact-head evidence records all required checks and residuals, with no P31/native relaxation scope leakage or release action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| P29 report → P30 restriction map | Conflict members and provenance cross a semantic API boundary. |
| Qualification fixtures → evidence ledger | Test results become release/merge claims. |
| Public docs → user expectations | Guarantees and unsupported/deferred capabilities are communicated externally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-11 | Tampering | P29-to-P30 origin conversion | high | mitigate | Exact identity checks, all-or-error conversion, and unsupported-origin negative corpus. |
| T-30-12 | Repudiation | qualification evidence | high | mitigate | Exact-head SHA, command/output ledger, named leaf mapping, and independent review disposition. |
| T-30-13 | Information disclosure | public example/docs | low | mitigate | Document only solver-neutral behavior; exclude credentials, machine paths, and native installation details. |
| T-30-14 | Elevation of privilege | guarantee labels | high | mitigate | Explicitly prohibit minimum-repair claims and distinguish native/portable/unknown/operational results. |
</threat_model>

<verification>
<automated>cargo fmt --all -- --check && cargo test -p roml --all-targets && cargo test -p roml-highs --all-targets && cargo package --list -p roml</automated>
</verification>

<success_criteria>
P30's positive closure predicate is executable: persistent algebra and portable weighted-L1 outcomes are qualified, supported P29 origins map exactly, unsupported origins reject, cleanup errors are retained, public behavior is documented, and the exact-head review/evidence gate is ready for owner merge.
</success_criteria>

<output>
Create `.planning/phases/30-soft-constraints/30-04-SUMMARY.md` when done.
</output>
