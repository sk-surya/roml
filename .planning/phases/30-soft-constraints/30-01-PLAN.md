---
phase: 30-soft-constraints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/construct/mod.rs
  - src/construct/soft_constraint.rs
  - src/model/mod.rs
  - src/lib.rs
  - src/advanced.rs
  - tests/soft_constraints_contract.rs
  - tests/soft_constraints_lifecycle.rs
  - docs/release/evidence/P30_SOFT_CONSTRAINTS_API.md
autonomous: true
requirements: [SM-10.1, SM-10.3, SM-10.4]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A caller can soften an existing live constraint through one checked builder call and receive a stable SoftConstraint handle."
    - "Persistent softening is canonical model state: successful additions advance the model revision and survive snapshot, delta, clone, and rebuild paths."
    - "Violation roles identify the originating constraint and are not confused with temporary solve-overlay artifacts."
    - "Invalid, stale, inactive, or unsupported inputs fail before any model, changelog, or revision mutation."
  artifacts:
    - path: "src/construct/soft_constraint.rs"
      provides: "Canonical soft-constraint payload, violation policy, penalty policy, and stable violation-role definitions."
      contains: "SoftConstraint"
    - path: "src/construct/mod.rs"
      provides: "Public SoftConstraint construct kind and exports through the existing generation-safe construct arena."
      contains: "SoftConstraint"
    - path: "src/model/mod.rs"
      provides: "Checked one-call softening builder and atomic model mutation/revision integration."
      contains: "soften"
    - path: "tests/soft_constraints_contract.rs"
      provides: "Compile-target API and negative-surface coverage for the frozen P30 boundary."
    - path: "tests/soft_constraints_lifecycle.rs"
      provides: "Canonical lifecycle, identity, revision, snapshot, delta, clone, and atomicity regression coverage."
  key_links:
    - from: "Model::soften_constraint"
      to: "ConstructStore + Change::ConstructAdded"
      via: "one prevalidated mutation inserts the payload and records the construct operation"
    - from: "Model::take_snapshot / DeltaBatch"
      to: "SoftConstraint construct entry"
      via: "existing canonical construct entry and revisioned semantic serialization paths"
    - from: "SoftConstraint violation roles"
      to: "original Constraint handle"
      via: "stable role metadata retained in the construct payload"
---

<objective>
Freeze the P30 public softening vocabulary and prove one canonical persistent soft-constraint path through the model, construct arena, revision, snapshot, and delta seams.

Purpose: D-01 and D-13 require softening to be a first-class semantic construct with no duplicate policy surface, while the later compiler and repair plans need exact stable handles and role metadata.
Output: a checked `SoftConstraint` builder/payload, public exports, lifecycle tests, and API characterization evidence.
</objective>

<execution_context>
@/home/skrishnan/.codex/gsd-core/workflows/execute-plan.md
@/home/skrishnan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/M3-semantic-modeling-workflows/SHARED-CONTRACTS.md
@.planning/phases/30-soft-constraints/30-CONTEXT.md
@.planning/phases/30-soft-constraints/30-PLAN.md
@src/construct/mod.rs
@src/model/mod.rs
@src/snapshot.rs
@src/delta.rs

<interfaces>
Existing seams to use directly: `ConstructKind`/`ConstructEntry` and `ConstructStore` in `src/construct/mod.rs`; `Model::add_construct_allocated`, `Model::take_snapshot`, `Model::deltas_since`, and `ModelError` in `src/model/mod.rs`; `ModelSnapshot.constructs`; `ModelOp::{AddConstruct,RemoveConstruct,SetConstructActive}`; and `FormulationPreference` as the existing per-construct policy. Do not add a second construct arena or a second objective-policy type.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 30-00: Freeze the P30 API and wire one persistent softening path</name>
  <reversibility rating="costly">The public builder and report vocabulary is consumed by the compiler, solver, and P34 evidence ledger; changing names or ownership after those consumers land requires coordinated API migration.</reversibility>
  <files>src/construct/mod.rs, src/construct/soft_constraint.rs, src/model/mod.rs, src/lib.rs, src/advanced.rs, tests/soft_constraints_contract.rs, docs/release/evidence/P30_SOFT_CONSTRAINTS_API.md</files>
  <behavior>
    - Test 1: a live primitive constraint can be softened once and returns a stable `SoftConstraint` handle plus lower/upper violation-role handles.
    - Test 2: the compile-target surface exposes `SoftConstraint`, `ViolationPolicy`, `PenaltyPolicy`, `PenaltyTarget::{None,Objective}`, `RelaxationRestriction`, and `RelaxationScope` without introducing priority targeting or a generic provider escape hatch.
    - Test 3: the new construct is present in the next canonical snapshot and delta batch with the same originating constraint identity.
  </behavior>
  <action>Per D-01, D-11, D-13, and D-15, define the exact P30 public types from the authoritative `30-PLAN.md` contract in `src/construct/soft_constraint.rs`, add the `SoftConstraint` variant and violation-role markers to the existing construct extension surface, and expose only the approved names through `src/lib.rs`/`src/advanced.rs`. Add the smallest production-quality builder path from an existing `Constraint` to a canonical construct entry: validate all referenced IDs and policy values before reserving or mutating state, allocate stable opaque construct/violation identities through the existing identity seams, attach the original constraint, and record the ordinary construct changelog operation so snapshot/delta/revision behavior is real. Keep `PenaltyTarget` limited to `None` and `Objective`; do not alter the existing P28 `ObjectivePolicy` or create `ObjectivePriority`. Record the exact baseline SHA, API inventory, and SM-10.6 ownership split in the evidence file.</action>
  <verify>
    <automated>cargo test -p roml --test soft_constraints_contract --test soft_constraints_lifecycle -- --nocapture</automated>
  </verify>
  <done>The frozen P30 types compile from an external-style test, one softening call reaches canonical state, the construct is revisioned and snapshot/delta-visible, and no invalid public policy surface is introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 30-01: Expand persistent softening lifecycle and atomic validation</name>
  <files>src/construct/soft_constraint.rs, src/model/mod.rs, tests/soft_constraints_lifecycle.rs</files>
  <behavior>
    - Test 1: clone preserves lineage and the soft construct's semantic content while the clone receives the existing distinct model instance identity.
    - Test 2: remove/deactivate and stale construct/constraint handles return typed errors and never leave orphaned violation roles or metadata.
    - Test 3: invalid cap, inactive constraint, foreign/stale handle, unsupported relation, and invalid penalty input leave model revision, snapshot, changelog, and construct count unchanged.
  </behavior>
  <action>Per D-01 and D-03, complete the builder validation matrix for the canonical construct: accept only live supported constraints, validate `max_violation` as finite and nonnegative when supplied, validate `PenaltyTarget::Objective` against a live objective, derive parameter dependencies from the payload, and make every rejection atomic before any identity reservation that would create observable state. Thread clone, remove, activity, invariant, and metadata behavior through the existing construct arena rather than adding side maps. Add explicit accessors needed by later compiler/solution tasks for the original constraint and each violation role, with stable ordering independent of hash-map iteration.</action>
  <verify>
    <automated>cargo test -p roml --test soft_constraints_lifecycle -- --nocapture</automated>
  </verify>
  <done>Persistent softening has complete typed lifecycle/atomicity coverage, stable role accessors, and no canonical-state operation is lost or silently accepted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller → model builder | User-supplied handles, bounds, weights, and policy values enter canonical state. |
| Canonical model → snapshot/delta | Semantic construct data crosses into compiler-facing immutable artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-01 | Tampering | `Model::soften_constraint` | high | mitigate | Validate every handle/value before mutation and test revision/changelog atomicity. |
| T-30-02 | Elevation of privilege | construct/origin identity | medium | mitigate | Use existing opaque generation-safe handles and reject stale/foreign identities. |
| T-30-03 | Information disclosure | diagnostics/report handles | low | accept | Reports expose opaque semantic identities already used by ROML; no native credentials or paths enter this surface. |
</threat_model>

<verification>
<automated>cargo fmt --all -- --check && cargo test -p roml --test soft_constraints_contract --test soft_constraints_lifecycle</automated>
</verification>

<success_criteria>
The P30 API is frozen without P31 ownership leakage; one-call persistent softening is canonical, revisioned, snapshot/delta-visible, stable, and atomically validated.
</success_criteria>

<output>
Create `.planning/phases/30-soft-constraints/30-01-SUMMARY.md` when done.
</output>
